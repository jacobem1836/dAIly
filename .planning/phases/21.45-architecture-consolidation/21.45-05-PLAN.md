---
phase: 21.45-architecture-consolidation
plan: 05
type: execute
wave: 2
depends_on: ["21.45-01"]
files_modified:
  - tests/e2e/test_smoke_pairing_to_voice.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "An E2E smoke test exercises the production voice path: pair → token → room join → agent greeting → one approval-interrupt round-trip"
    - "The approval round-trip runs against the REAL orchestrator graph (not a mock graph) and fails if the greeting or approval path breaks"
    - "The test runs in CI under pytest"
  artifacts:
    - path: "tests/e2e/test_smoke_pairing_to_voice.py"
      provides: "Extended smoke test asserting agent greeting + approval interrupt resume"
      contains: "interrupt"
  key_links:
    - from: "tests/e2e/test_smoke_pairing_to_voice.py"
      to: "src/daily/orchestrator/graph.py"
      via: "real build_graph + interrupt()/Command(resume) round-trip"
      pattern: "build_graph"
---

<objective>
Add the ONE missing E2E smoke test for the production voice path so live-session whack-a-mole becomes CI signal. The existing test (`tests/e2e/test_smoke_pairing_to_voice.py`) already covers pair → Google OAuth → prefs → `/livekit/token` → refresh, but STOPS at token issuance — it never exercises the worker agent greeting or the approval interrupt. This plan EXTENDS that test (not a new file) to: join a LiveKit room (boundary faked) → assert the worker agent delivers a greeting → drive one approval-interrupt round-trip (draft → `interrupt()` → resume confirm) against the REAL orchestrator graph. Depends on Plan 01 so the voice path is unified (single worker/livekit-agents path on AsyncPostgresSaver) before the test asserts against it.

Purpose: Catch the only failure class that actually ships (cross-service E2E), on the consolidated single voice path.
Output: Extended smoke test that passes in CI and fails if the greeting or approval round-trip regresses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.45-architecture-consolidation/21.45-CONTEXT.md
@.planning/reviews/2026-06-19-integration-friction-review.md
@.planning/phases/21.45-architecture-consolidation/21.45-01-PLAN.md
@tests/e2e/test_smoke_pairing_to_voice.py
@src/daily/worker/voice_pipeline.py
@src/daily/worker/llm_bridge.py
@src/daily/orchestrator/graph.py

<interfaces>
<!-- Grounded against the live tree on 2026-06-19. -->

Existing test (tests/e2e/test_smoke_pairing_to_voice.py):
- `@pytest.mark.e2e async def test_full_user_onboarding_to_voice_connect(client, mock_resend, mock_oauth_exchange, db_factory)`.
- `client` fixture yields `(ac, fake_redis, db_factory)` — httpx AsyncClient against the full FastAPI app, fakeredis already wired.
- Currently ends at Step 8 (`POST /livekit/token` → JWT-shaped token) and Step 9 (refresh). EXTEND beyond Step 8.
- External services mocked at HTTP/SDK boundary; fakeredis is a dev dep already in use.

Production voice path to exercise (post-Plan-01, the SINGLE path):
- `src/daily/worker/voice_pipeline.py` — livekit-agents VoicePipelineAgent; delegates STT/TTS/barge-in to the library. MOCK the LiveKit room + livekit-agents pipeline at THIS boundary.
- `src/daily/worker/llm_bridge.py` — bridges the agent turn into the orchestrator graph (the seam where a user utterance → graph invocation happens). The greeting + the user turn that triggers a draft go through here.
- `src/daily/orchestrator/graph.py` — `build_graph(...)`; the approval `interrupt()` lives in the orchestrator nodes (`orchestrator/nodes.py`, `session.py`). Use the REAL graph (per CONTEXT: "must exercise the orchestrator approval interrupt with a real graph, not a mock graph"). The approval gate uses LangGraph `interrupt()` + `Command(resume=...)`.

CONTEXT decision (workstream 2): EXTEND the existing test, do not duplicate. Fake LiveKit/livekit-agents externalities at the boundary; the orchestrator graph must be REAL. The exact mock boundary for livekit-agents is Claude's discretion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the smoke test — assert worker agent greeting after room join</name>
  <behavior>
    - After issuing the LiveKit token (existing Step 8), the test simulates the worker agent joining the room (livekit-agents pipeline faked at its boundary) and asserts a greeting is produced for the paired user.
    - The greeting path runs through the real worker→orchestrator bridge (llm_bridge) so a regression in greeting generation fails the test.
    - LiveKit room transport and the livekit-agents audio pipeline are mocked; the orchestrator graph is real (AsyncPostgresSaver or the test's async DB).
  </behavior>
  <read_first>
    - tests/e2e/test_smoke_pairing_to_voice.py (the function to extend; conftest fixtures it depends on)
    - src/daily/worker/voice_pipeline.py (where the agent is constructed/joins — the mock boundary)
    - src/daily/worker/llm_bridge.py (greeting → graph seam)
    - tests/e2e/ conftest (or tests/conftest.py) for the `client`, `db_factory`, fakeredis fixtures
    - .planning/phases/21.45-architecture-consolidation/21.45-CONTEXT.md (<decisions> → "E2E smoke test")
  </read_first>
  <action>
    Extend `test_full_user_onboarding_to_voice_connect` (or add a sibling `async def` in the SAME file that reuses the pairing setup — do NOT create a new file):
    1. After Step 8 (token issued), construct the worker voice pipeline / agent for the paired user, mocking the LiveKit room + livekit-agents pipeline at their boundary (e.g. patch the room connect + the agent's TTS/STT plumbing so no real audio/socket is needed). The orchestrator graph must be the REAL `build_graph(...)` using the test's async DB/checkpointer.
    2. Drive the agent's greeting step and assert a non-empty greeting is produced and routed to the (faked) TTS/room output for the paired user. Assert it actually went through `llm_bridge` → orchestrator, not a hardcoded string.
    3. Use the existing fixtures (`client`, `db_factory`, fakeredis); add new fixtures/mocks only for the LiveKit/livekit-agents boundary. Keep OpenAI/LLM calls mocked deterministically (the orchestrator nodes call the LLM — patch the LLM client to return canned structured responses so the graph logic is real but the model is faked).
  </action>
  <verify>
    <automated>pytest tests/e2e/test_smoke_pairing_to_voice.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The test simulates room join and asserts the worker agent produces a greeting for the paired user.
    - The greeting flows through the real worker→orchestrator bridge (llm_bridge + real build_graph), with only LiveKit/livekit-agents and the LLM model mocked.
    - `pytest tests/e2e/test_smoke_pairing_to_voice.py -q` exits 0.
    - No duplicate smoke-test file was created (still one file).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert one approval-interrupt round-trip against the real graph</name>
  <behavior>
    - The test drives a user turn that produces a draft action, hits the orchestrator approval `interrupt()`, then resumes with a confirm and asserts the action proceeds to execution (approval_status reflects approved/executed).
    - The interrupt + resume runs against the REAL LangGraph graph (interrupt() / Command(resume=...)), not a mock graph.
    - The test FAILS if the approval gate is bypassed (action executes without interrupt) or if resume doesn't advance the graph.
  </behavior>
  <read_first>
    - tests/e2e/test_smoke_pairing_to_voice.py (extended in Task 1)
    - src/daily/orchestrator/graph.py, src/daily/orchestrator/nodes.py, src/daily/orchestrator/session.py (approval interrupt() + Command(resume) wiring)
    - src/daily/actions/ (approval-gated email/calendar action + action_log approval_status)
    - src/daily/worker/llm_bridge.py (how a user turn drives the graph to the interrupt)
    - .planning/reviews/2026-06-19-integration-friction-review.md (Approval workflow flow; P5 HITL primitive)
  </read_first>
  <action>
    Continue the same test function/file:
    1. Feed a user utterance that triggers a draft (e.g. "reply to that email saying yes") through the worker→orchestrator bridge. The orchestrator graph runs draft → approval node and raises `interrupt()` — assert the graph is paused awaiting approval (the interrupt payload / pending approval is observable), and that NO external action executed yet.
    2. Resume with `Command(resume=<confirm>)` (approval granted) and assert the graph advances to execute. The external action adapter (Google/Microsoft) is mocked at its SDK boundary; assert it was invoked exactly once and that `action_log.approval_status` reflects approved/executed.
    3. Add a guard assertion that the approval gate is real: confirm execution did NOT happen before the resume (i.e. the interrupt genuinely blocked it). If the action fires without the resume, the test must fail.
    4. Keep the LLM mocked deterministically so the draft content is stable; the graph control flow and interrupt/resume are real.
    5. Ensure the test carries `@pytest.mark.e2e` and runs under the default CI pytest selection (confirm the e2e marker is collected in CI; if e2e is excluded by default, note it and add the marker to the CI run set).
  </action>
  <verify>
    <automated>pytest tests/e2e/test_smoke_pairing_to_voice.py -q && grep -q "interrupt\|resume\|Command" tests/e2e/test_smoke_pairing_to_voice.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - The test drives draft → `interrupt()` → `Command(resume)` → execute against the REAL orchestrator graph.
    - It asserts the action did NOT execute before resume (the gate is genuinely enforced) and executed exactly once after resume, with `approval_status` updated.
    - Only LiveKit/livekit-agents, the LLM model, and the external action SDK are mocked; graph + interrupt logic are real.
    - `pytest tests/e2e/test_smoke_pairing_to_voice.py -q` exits 0, and the file references `interrupt`/`resume`/`Command`.
    - The test is collected by CI (e2e marker in the CI run set); SUMMARY confirms the CI selection.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pytest tests/e2e/test_smoke_pairing_to_voice.py -q` exits 0.
- The file asserts a greeting AND an approval round-trip (`grep -E "interrupt|resume|Command|greeting" tests/e2e/test_smoke_pairing_to_voice.py`).
- The orchestrator graph used is the real `build_graph` (no mock-graph substitution); only LiveKit/livekit-agents, the LLM, and the external action SDK are faked.
- Negative assertion present: action does not execute before resume.
- Test runs under the CI pytest selection (e2e marker collected).
</verification>

<success_criteria>
A single E2E smoke test exercises the full production voice path — pair → token → room join → worker agent greeting → one approval-interrupt round-trip — against the real orchestrator graph, with externalities faked at their boundaries. It runs in CI and fails on regression of the greeting or approval path. Built on the unified single voice path from Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/21.45-architecture-consolidation/21.45-05-SUMMARY.md`. Record the mock boundary chosen for LiveKit/livekit-agents and the LLM, how the real interrupt/resume was driven, the negative-gate assertion, and confirmation the test runs in CI.
</output>
