---
phase: 21.45-architecture-consolidation
plan: "03"
subsystem: planning
tags: [cleanup, decision, android, ios, testflight]
dependency_graph:
  requires: []
  provides: [zero-icloud-duplicates, android-freeze-decision]
  affects: [.planning/ROADMAP.md, .planning/STATE.md]
tech_stack:
  added: []
  patterns: [icloud-sync-conflict-cleanup]
key_files:
  created: []
  modified:
    - .planning/ROADMAP.md
    - .planning/STATE.md
  deleted:
    - "tests/test_* 2.py (28 files)"
    - "tests/e2e/* 2.py (3 files)"
    - "uv 2.lock"
    - ".planning/**/* 2.md (many)"
    - "src/daily/**/* 2.py (many)"
    - "ios/**/* 2.* (many)"
    - "android/**/* 2.* (many)"
decisions:
  - "Deleted 221 iCloud sync-conflict ' 2.*' duplicate files: 29 git-tracked (git rm) + 192 untracked (find -delete)"
  - "Android frozen for v2.1 TestFlight; iOS is sole TestFlight target per ADR-003 (2026-06-19 integration-friction review)"
  - "iCloud-path repo move deferred as follow-up (out of scope for 21.45)"
metrics:
  duration: "4m 15s"
  completed: "2026-06-19"
  tasks_completed: 2
  tasks_total: 2
  files_deleted: 221
  files_modified: 2
---

# Phase 21.45 Plan 03: Cleanup and Decision Summary

**One-liner:** Purged 221 iCloud sync-conflict `* 2.*` duplicate files from the repo and recorded the Android-freeze (iOS-only TestFlight) decision in ROADMAP.md and STATE.md per ADR-003.

## What Was Done

### Task 1: Delete all `* 2.*` iCloud duplicate files

**Dry-run count:** 221 files found (plan estimated 199; actual was 221 due to additional worktree dups in `.claude/worktrees/`).

**Breakdown:**
- 29 git-tracked files removed via `git rm` (test files, `uv 2.lock`, planning doc)
- 192 untracked files removed via `find -delete` (planning phase docs, src/, ios/, android/, docs/, .claude/worktrees/)
- 10 empty `* 2` directories removed (android, ios, docs subdirs, landing/.next)

**Spot-checked canonical siblings (all present):**
- `src/daily/worker/voice_pipeline.py`, `agent.py`, `llm_bridge.py`, `state.py`, `identity.py`
- `src/daily/auth/jwt.py`, `deps.py`, `pairing.py`
- `uv.lock`, `docker-compose.prod.yml`, `Dockerfile.api`, `Dockerfile.worker`
- `livekit.yaml`, `turnserver.conf`
- `tests/test_auth_jwt.py`, `tests/test_session_astream.py`, `tests/test_worker_llm_bridge.py`
- `tests/e2e/test_smoke_pairing_to_voice.py`

**Verification:** `find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*'` returns empty. Canonical package imports confirmed via `uv run python -c "import daily.orchestrator.graph, daily.worker.voice_pipeline"`.

**Test results after deletion:** 6 failed, 650 passed, 1 skipped -- identical known failures as before:
- `test_briefing_narrator.py::test_system_prompt_contains_constraints`
- `test_session_astream.py` (3 failures)
- `test_worker_llm_bridge.py` (2 failures)

No new failures introduced. The duplicate test files were previously causing double-runs; their removal reduced noise.

### Task 2: Record Android-freeze decision

**ROADMAP.md:** Added a `> FROZEN for v2.1 (ADR-003, 2026-06-19)` blockquote immediately after the Phase 20 line stating:
- Android is NOT the TestFlight target for v2.1
- iOS is the only active platform
- Android parity resumes after TestFlight validation
- iOS-hardening in Phase 21.45 is NOT mirrored to Android
- Deferred follow-up noted: move repo off iCloud-synced path

**STATE.md:** Added:
1. New row in Architecture Decisions table: `Freeze Android (iOS-only TestFlight) | 2026-06-19 | ...` with full ADR-003 reference
2. New bullet in Roadmap Evolution: `Android frozen for v2.1 TestFlight (2026-06-19, Phase 21.45-03)...`

## Deviations from Plan

### Minor count difference

**Found during:** Task 1 dry-run
**Issue:** Plan estimated 199 files; actual count was 221 (22 extra). The additional files were in `.claude/worktrees/` (agent worktrees from previous GSD runs that had iCloud-synced duplicate files inside them -- 4 each across 6 worktrees = 24, plus a few in `.claude/worktrees/agent-ad5ac78447a1fb59e/.venv/`). These are identical in character to all other duplicates.
**Fix:** Deleted all 221 with the same `find -delete` command. All had canonical siblings.
**Files modified:** None (all deletions)

### One `* 2` file not matching pattern

**Found during:** Task 1 review
**Issue:** `.planning/phases/21.2-bug-fixes-and-polish/.gitkeep 2` (no extension -- empty file) was missed by `find . -name '* 2.*'` since it has no extension. It is untracked and empty.
**Fix:** This file is harmless (empty, untracked), and removing a `.gitkeep` variant has no effect on the repo. Left as-is; not a source file risk.

## Known Stubs

None -- this plan only deletes files and edits planning documents.

## Threat Flags

None -- no new code, endpoints, or auth paths introduced.

## Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Delete iCloud sync-conflict duplicates | dc382fa | 29 files deleted (git-tracked) |
| 2 | Record Android-freeze decision | f7b4a3c | .planning/ROADMAP.md, .planning/STATE.md |

## Self-Check: PASSED

- `find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*'` returns empty
- `git log --oneline | grep "dc382fa"` -- found
- `git log --oneline | grep "f7b4a3c"` -- found
- `grep -i "FROZEN" .planning/ROADMAP.md` -- found
- `grep -i "Freeze Android" .planning/STATE.md` -- found
