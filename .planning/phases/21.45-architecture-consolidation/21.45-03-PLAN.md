---
phase: 21.45-architecture-consolidation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Zero iCloud sync-conflict duplicate files (* 2.*) remain in the repo (excluding .git/.venv)"
    - "The Android-freeze decision is recorded in ROADMAP.md and STATE.md as the iOS-only TestFlight target for this milestone"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Recorded Android-freeze decision under Phase 20 / v2.1"
      contains: "Android"
    - path: ".planning/STATE.md"
      provides: "Android-freeze decision in Architecture Decisions / Roadmap Evolution"
      contains: "Android"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "consistent Android-freeze decision recorded in both"
      pattern: "freeze"
---

<objective>
Remove the 199 iCloud sync-conflict duplicate files (`* 2.*`) polluting the repo, and formally record the decision to FREEZE Android development (iOS is the only TestFlight target for this milestone). The duplicates (e.g. `agent 2.py`, `voice_pipeline 2.py`, `docker-compose.prod 2.yml`, `uv 2.lock`, plus ~190 more) are noise created by iCloud sync conflicts and risk accidental edits to the wrong file. Android voice plumbing exists but has no onboarding/integration/permissions UI; maintaining parity before iOS is validated is a tax we are explicitly not paying now.

Purpose: De-noise the working tree and lock the iOS-only scope so downstream work (and Phase 21.5) doesn't re-litigate Android parity.
Output: Zero `* 2.*` files; Android-freeze decision recorded in ROADMAP.md and STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21.45-architecture-consolidation/21.45-CONTEXT.md
@.planning/reviews/2026-06-19-integration-friction-review.md

<interfaces>
<!-- Grounded against the live tree on 2026-06-19. -->
- `find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*'` currently returns 199 files.
- These are iCloud sync-conflict duplicates (filename contains " 2" before the extension). The canonical file (without " 2") exists alongside each.
- Examples: ./docker-compose.prod 2.yml, ./uv 2.lock, ./livekit 2.yaml, ./turnserver 2.conf, ./Dockerfile 2.api, ./Dockerfile 2.worker, src/daily/worker/agent 2.py, src/daily/worker/voice_pipeline 2.py, tests/test_auth_jwt 2.py, and the staged ".planning/phases/19-.../...19-01-PLAN 2.md" set seen in git status.
- ROADMAP.md: Phase 20 (Native Android App) is at line ~82; the v2.1 milestone table includes Phase 21.45 / 21.5 at lines ~258-259. STATE.md has "Architecture Decisions" (~line 45) and "Roadmap Evolution" (~line 72) sections.
- Moving the repo OFF the iCloud-synced path is explicitly OUT of scope (deferred) — only note it as a follow-up.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run then delete all "* 2.*" iCloud duplicate files</name>
  <read_first>
    - .planning/phases/21.45-architecture-consolidation/21.45-CONTEXT.md (<decisions> → "DELETE the iCloud sync-conflict duplicate files")
    - .planning/reviews/2026-06-19-integration-friction-review.md §7 ("199 * 2.* duplicate files")
  </read_first>
  <action>
    1. DRY-RUN FIRST — list the duplicates and capture the count so the SUMMARY can record what was removed:
       ```
       find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*' | tee /tmp/icloud_dups.txt | wc -l
       ```
       Sanity-check the list: each `* 2.*` should have a canonical sibling without " 2". Spot-check 3-4 (e.g. confirm `src/daily/worker/voice_pipeline.py` exists next to `voice_pipeline 2.py`). Do NOT delete any file whose canonical sibling is missing — if any orphan duplicate exists (no non-" 2" version), surface it in the SUMMARY and skip just that file.
    2. DELETE:
       ```
       find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*' -delete
       ```
       Note: some duplicates are already tracked/staged in git (the `.planning/phases/19-.../...PLAN 2.md` set). `-delete` removes them from disk; the subsequent commit (handled by the orchestrator) will drop them from the index. If `git rm` is needed for tracked ones, run `git rm --cached` on the tracked " 2" paths after the find-delete, or let the commit pick up the deletions.
    3. VERIFY zero remain:
       ```
       find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*'
       ```
       Must return nothing.
    4. Confirm no canonical file was harmed: re-import the Python package as a smoke check: `python -c "import daily.orchestrator.graph, daily.worker.voice_pipeline"`.
  </action>
  <verify>
    <automated>test -z "$(find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*')" && python -c "import daily.orchestrator.graph, daily.worker.voice_pipeline" && echo "PASS: zero dups, package imports"</automated>
  </verify>
  <acceptance_criteria>
    - `find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*'` returns nothing (zero matches).
    - `/tmp/icloud_dups.txt` recorded the original ~199-file list for the SUMMARY.
    - No canonical (non-" 2") file was deleted — `python -c "import daily.orchestrator.graph, daily.worker.voice_pipeline"` still imports.
    - Any orphan duplicate (no canonical sibling) was skipped and surfaced, not blindly deleted.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record the Android-freeze decision in ROADMAP.md and STATE.md</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 20 section ~line 82; v2.1 milestone table ~258-259)
    - .planning/STATE.md (Architecture Decisions table ~line 45; Roadmap Evolution ~line 72)
    - .planning/phases/21.45-architecture-consolidation/21.45-CONTEXT.md (<decisions> → "FREEZE Android development")
    - .planning/reviews/2026-06-19-integration-friction-review.md ADR-003 (iOS-first; freeze Android)
  </read_first>
  <action>
    Record the decision in BOTH planning docs, consistently:
    1. In `.planning/ROADMAP.md`: under Phase 20 (Native Android App) and/or the v2.1 milestone notes, add a clear note: "FROZEN for v2.1 — iOS is the only TestFlight target for this milestone. Android voice plumbing exists but onboarding/integration/permissions UI is absent; parity deferred until iOS is validated with real users (per ADR-003, integration-friction review 2026-06-19). iOS-hardening changes in Phase 21.45 are NOT mirrored to Android."
    2. In `.planning/STATE.md`: add a row to the Architecture Decisions table (Decision: "Freeze Android", Date: 2026-06-19, Details: "iOS-only TestFlight for v2.1 milestone; Android parity deferred per ADR-003") AND a bullet under Roadmap Evolution noting the freeze.
    3. Also note in ROADMAP (as a deferred follow-up, NOT a task) that moving the repo off the iCloud-synced path remains an open follow-up (out of scope for 21.45) — this is the source of the duplicate-file problem.
    4. Keep wording consistent between the two files (same decision, same date, same ADR reference).
  </action>
  <verify>
    <automated>grep -qi "freeze.*android\|android.*freeze\|android.*frozen\|frozen.*android\|iOS.*only.*TestFlight" .planning/ROADMAP.md && grep -qi "android" .planning/STATE.md && grep -qi "2026-06-19" .planning/STATE.md && echo "PASS: decision recorded in both"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` states Android is frozen and iOS is the only TestFlight target for the milestone, referencing ADR-003 / the 2026-06-19 review.
    - `.planning/STATE.md` has an Android-freeze entry in the Architecture Decisions table dated 2026-06-19 and a Roadmap Evolution note.
    - Both files use consistent wording; the iCloud-path move is noted as a deferred follow-up in ROADMAP.
    - grep for the freeze decision succeeds in both files.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `find . -name '* 2.*' -not -path './.git/*' -not -path './.venv/*'` → 0 matches.
- `python -c "import daily.orchestrator.graph, daily.worker.voice_pipeline"` exits 0 (no canonical file destroyed).
- `grep -i "android" .planning/ROADMAP.md` and `.planning/STATE.md` both show the freeze decision.
- STATE.md Architecture Decisions table contains a 2026-06-19 Android-freeze row.
</verification>

<success_criteria>
Zero `* 2.*` duplicate files remain; no canonical file was harmed. The Android-freeze (iOS-only TestFlight) decision is recorded consistently in ROADMAP.md and STATE.md with ADR-003 reference, and the iCloud-path move is logged as a deferred follow-up.
</success_criteria>

<output>
After completion, create `.planning/phases/21.45-architecture-consolidation/21.45-03-SUMMARY.md`. Record the duplicate-file count removed, any orphans skipped, and the exact wording/location of the Android-freeze decision in both docs.
</output>
