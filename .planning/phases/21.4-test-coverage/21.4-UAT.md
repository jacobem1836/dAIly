---
status: partial
phase: 21.4-test-coverage
source: [21.4-01-SUMMARY.md, 21.4-02-SUMMARY.md, 21.4-03-SUMMARY.md]
started: 2026-05-12T00:00:00Z
updated: 2026-05-12T01:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Backend test suite passes at 80%+ coverage
expected: Running `uv run pytest --cov=src/daily/auth --cov=src/daily/integrations -q --tb=short` exits 0, reports 80%+ total coverage, and all tests pass (approx 776 tests).
result: pass

### 2. Coverage floor enforced in pyproject.toml
expected: `grep 'fail_under' pyproject.toml` returns `fail_under = 80`. Running the full suite without the --cov flag still passes. Adding a new function without a test and running `uv run pytest --cov=src/daily/auth --cov-fail-under=80` would fail (coverage gate is active).
result: pass

### 3. E2E smoke test passes via pytest -m e2e
expected: Running `uv run pytest -m e2e -v` exits 0 in under 5 seconds. Output shows `test_full_user_onboarding_to_voice_connect PASSED`. All 9 endpoint assertions pass (send-link, pair, Google connect, Google callback, preferences, integrations, livekit token, refresh).
result: pass

### 4. iOS test files exist and are substantive
expected: Three XCTest files exist under `ios/dAIlyTests/`: OnboardingFlowTests.swift (12 tests), DeepLinkRoutingTests.swift (7 tests), AppStateRoutingTests.swift (6 tests). Running `grep -c 'func test' ios/dAIlyTests/OnboardingFlowTests.swift` returns 12.
result: pass

### 5. iOS test suite builds and passes (Xcode required)
expected: Running `xcodebuild test -project ios/dAIly.xcodeproj -scheme dAIly -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:dAIlyTests` shows TEST SUCCEEDED with all 25 new tests plus prior tests passing, no regressions.
result: blocked
blocked_by: physical-device
reason: iOS Simulator requires Xcode environment — not available in CLI session.

## Summary

total: 5
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

[none yet]
