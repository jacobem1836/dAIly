---
phase: 21.4-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_auth_router_coverage.py
  - tests/test_google_oauth_coverage.py
  - tests/test_microsoft_oauth_coverage.py
  - tests/test_slack_oauth_coverage.py
autonomous: true
requirements:
  - TEST-BACKEND-01
  - TEST-BACKEND-02
must_haves:
  truths:
    - "src/daily/auth/router.py reaches 80%+ line coverage"
    - "src/daily/integrations/google/auth.py reaches 80%+ line coverage"
    - "src/daily/integrations/microsoft/auth.py reaches 80%+ line coverage"
    - "src/daily/integrations/slack/auth.py reaches 80%+ line coverage"
    - "Total backend coverage stays at 80%+ and is stable (not borderline)"
  artifacts:
    - path: "tests/test_auth_router_coverage.py"
      provides: "Coverage tests for token/refresh and edge cases in auth router"
    - path: "tests/test_google_oauth_coverage.py"
      provides: "Coverage tests for Google OAuth flow paths"
    - path: "tests/test_microsoft_oauth_coverage.py"
      provides: "Coverage tests for Microsoft OAuth flow paths"
    - path: "tests/test_slack_oauth_coverage.py"
      provides: "Coverage tests for Slack OAuth flow paths"
  key_links:
    - from: "tests/test_auth_router_coverage.py"
      to: "src/daily/auth/router.py"
      via: "httpx ASGITransport against FastAPI app"
      pattern: "from daily.auth"
---

<objective>
Lift backend coverage from a borderline 80% total to a robust 80%+ on every critical OAuth/auth module. Currently `auth/router.py` is at 54%, Google OAuth auth at 48%, Microsoft OAuth auth at 47%, Slack OAuth auth at 40%. These are exactly the surfaces that gate every new user — they must be tested before TestFlight.

Purpose: Production readiness. Auth and OAuth are user-facing; regressions silently lock people out.
Output: Four new test files covering uncovered branches in auth + integrations OAuth modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/conftest.py
@tests/test_auth_pairing.py
@tests/test_google_oauth.py
@tests/test_microsoft_oauth.py
@tests/test_slack_oauth.py
@src/daily/auth/router.py
@src/daily/integrations/google/auth.py
@src/daily/integrations/microsoft/auth.py
@src/daily/integrations/slack/auth.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover auth router refresh + error paths</name>
  <files>tests/test_auth_router_coverage.py</files>
  <read_first>
    - src/daily/auth/router.py (read fully — every endpoint must be enumerated)
    - tests/test_auth_pairing.py (reuse fixtures: db_session_factory, client, _env)
    - tests/test_auth_send_link.py (existing send-link tests — do NOT duplicate)
    - tests/test_auth_jwt.py
  </read_first>
  <behavior>
    Add tests for branches NOT covered by existing test_auth_pairing.py / test_auth_send_link.py / test_auth_jwt.py:
    - test_refresh_token_invalid_returns_401: POST /auth/token/refresh with garbage refresh token → 401
    - test_refresh_token_revoked_returns_401: revoke a DeviceToken row, then POST /auth/token/refresh → 401
    - test_refresh_token_expired_returns_401: insert DeviceToken with past expires_at → 401
    - test_refresh_token_rotates_returned_token: successful refresh returns NEW access token; verify old != new
    - test_pair_complete_invalid_code_returns_400: POST /auth/pair/complete with wrong 6-digit code → 400/401
    - test_pair_complete_expired_code_returns_400: insert PairingCode with past expires_at, POST complete → 400/401
    - test_pair_initiate_unknown_user_returns_404: POST /auth/pair/initiate with user_id that doesn't exist → 404
    - test_send_link_resend_failure_still_returns_204: monkeypatch send_magic_link to raise ResendError → still 204 (no enumeration)
    Reuse the in-memory SQLite session_factory + client fixtures from test_auth_pairing.py (copy or import).
  </behavior>
  <action>
    Create tests/test_auth_router_coverage.py. Reuse the fixture pattern from tests/test_auth_pairing.py (in-memory SQLite, ASGITransport, monkeypatched async_session). Implement the 8 test functions listed in <behavior>. Each test must hit a real branch in auth/router.py that current tests miss. Use `coverage report --include='src/daily/auth/router.py'` to confirm uplift after writing. If a branch is genuinely unreachable from outside (e.g., logging-only path), skip it and document why in a module docstring at the top of the test file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auth_router_coverage.py -x -q && uv run coverage run -m pytest tests/test_auth_router_coverage.py tests/test_auth_pairing.py tests/test_auth_send_link.py tests/test_auth_jwt.py && uv run coverage report --include='src/daily/auth/router.py'</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_auth_router_coverage.py` exits 0
    - `grep -c "^def test_\|^async def test_" tests/test_auth_router_coverage.py` returns 8 or more
    - `coverage report --include='src/daily/auth/router.py'` shows coverage at 80% or higher
    - No xfail / skip markers on any test in this file
  </acceptance_criteria>
  <done>auth/router.py at 80%+ coverage, 8+ new tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover Google + Microsoft + Slack OAuth auth modules</name>
  <files>
    tests/test_google_oauth_coverage.py
    tests/test_microsoft_oauth_coverage.py
    tests/test_slack_oauth_coverage.py
  </files>
  <read_first>
    - src/daily/integrations/google/auth.py (every function and branch)
    - src/daily/integrations/microsoft/auth.py
    - src/daily/integrations/slack/auth.py
    - tests/test_google_oauth.py (existing — do NOT duplicate)
    - tests/test_microsoft_oauth.py
    - tests/test_slack_oauth.py
    - tests/test_integrations_router.py (for fixture patterns)
  </read_first>
  <behavior>
    For EACH provider (google, microsoft, slack), cover the uncovered branches in `auth.py`. Common targets:
    - test_<provider>_build_authorize_url_includes_state_and_scopes: verify URL has expected scopes + state param
    - test_<provider>_exchange_code_success: mock httpx response → returns token dict; assert token contents stored
    - test_<provider>_exchange_code_http_error: mock httpx 400 response → raises a domain error (verify type)
    - test_<provider>_exchange_code_network_error: mock httpx.RequestError → raises domain error
    - test_<provider>_refresh_access_token_success: mock refresh response → returns new token, expires_at updated
    - test_<provider>_refresh_access_token_invalid_grant: mock 400 invalid_grant → marks token revoked / raises
    - test_<provider>_token_persisted_encrypted: after exchange_code, decrypt and verify roundtrip via vault.crypto
    Slack-specific extras:
    - test_slack_user_token_vs_bot_token_split: Slack returns both; ensure correct one is stored per scope
    Microsoft-specific:
    - test_microsoft_tenant_in_authorize_url: verify `common` (or configured tenant) appears in authorize URL
  </behavior>
  <action>
    Create three separate test files (one per provider). Use httpx mock or respx (whichever is already in use — check tests/test_google_oauth.py first). Each provider's auth.py has roughly: build_authorize_url, exchange_code_for_token, refresh_access_token. Cover happy path + every error branch. Use `monkeypatch.setattr` on `httpx.AsyncClient` or use `respx` if already a dependency. Confirm encryption roundtrip by importing daily.vault.crypto and asserting decrypted bytes match original. After writing all three, run `coverage report --include='src/daily/integrations/*/auth.py'` to confirm each at 80%+.
  </action>
  <verify>
    <automated>uv run pytest tests/test_google_oauth_coverage.py tests/test_microsoft_oauth_coverage.py tests/test_slack_oauth_coverage.py -x -q && uv run coverage run -m pytest tests/test_google_oauth.py tests/test_google_oauth_coverage.py tests/test_microsoft_oauth.py tests/test_microsoft_oauth_coverage.py tests/test_slack_oauth.py tests/test_slack_oauth_coverage.py && uv run coverage report --include='src/daily/integrations/google/auth.py,src/daily/integrations/microsoft/auth.py,src/daily/integrations/slack/auth.py'</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_google_oauth_coverage.py tests/test_microsoft_oauth_coverage.py tests/test_slack_oauth_coverage.py` exits 0
    - Each new file has 6 or more `def test_` entries (`grep -c "def test_" tests/test_<provider>_oauth_coverage.py >= 6`)
    - `coverage report --include='src/daily/integrations/google/auth.py'` shows 80% or higher
    - `coverage report --include='src/daily/integrations/microsoft/auth.py'` shows 80% or higher
    - `coverage report --include='src/daily/integrations/slack/auth.py'` shows 80% or higher
  </acceptance_criteria>
  <done>All three OAuth auth modules at 80%+ coverage with passing tests.</done>
</task>

<task type="auto">
  <name>Task 3: Verify total backend coverage and freeze the floor</name>
  <files>tests/conftest.py (modify only if a fixture is added), pyproject.toml (only to set --cov-fail-under)</files>
  <read_first>
    - pyproject.toml (find the [tool.pytest.ini_options] / [tool.coverage] section)
    - tests/conftest.py
  </read_first>
  <action>
    Run the full suite with coverage. Confirm TOTAL is 80%+ and the four target modules are all 80%+. Then add `--cov-fail-under=80` to the pytest config (or `[tool.coverage.report] fail_under = 80`) so future regressions break CI. Do NOT change any source under src/ to chase coverage — only test files. If TOTAL dips below 80% after these additions (it shouldn't), STOP and report rather than rewriting source.
  </action>
  <verify>
    <automated>uv run coverage run -m pytest -q && uv run coverage report --fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - `coverage report --fail-under=80` exits 0
    - `coverage report` TOTAL line shows 80% or higher
    - `grep -E "fail_under|cov-fail-under" pyproject.toml` returns at least one match
    - No source file under `src/daily/` was modified by this plan (`git diff --stat src/` empty for this plan)
  </acceptance_criteria>
  <done>Coverage floor enforced at 80% in pyproject.toml; all tests pass; auth + OAuth modules each 80%+.</done>
</task>

</tasks>

<verification>
- All four target modules at 80%+ individual coverage
- TOTAL backend coverage 80%+
- Coverage floor enforced in pyproject.toml
- No source file mutated for coverage gaming
</verification>

<success_criteria>
- pytest exits 0 with `--cov-fail-under=80`
- auth/router.py, google/auth.py, microsoft/auth.py, slack/auth.py all individually 80%+
- 20+ new test functions added across this plan
</success_criteria>

<output>
After completion, create `.planning/phases/21.4-test-coverage/21.4-01-SUMMARY.md` documenting per-module coverage delta and total test count added.
</output>
