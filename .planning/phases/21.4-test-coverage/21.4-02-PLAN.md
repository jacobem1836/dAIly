---
phase: 21.4-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/dAIlyTests/OnboardingFlowTests.swift
  - ios/dAIlyTests/DeepLinkRoutingTests.swift
  - ios/dAIlyTests/AppStateRoutingTests.swift
autonomous: true
requirements:
  - TEST-IOS-01
  - TEST-IOS-02
must_haves:
  truths:
    - "Onboarding step transitions are unit-tested (welcome → integrations → schedule → permissions → completion)"
    - "Deep-link URLs (oauth callback, pair code) route to the correct AppState transitions"
    - "AppState routing logic (signed-out → pairing → onboarding → voice) is exercised in tests"
  artifacts:
    - path: "ios/dAIlyTests/OnboardingFlowTests.swift"
      provides: "Onboarding step machine + integration completion tracking tests"
    - path: "ios/dAIlyTests/DeepLinkRoutingTests.swift"
      provides: "URL handling tests for /oauth/success and /pair/* deep links"
    - path: "ios/dAIlyTests/AppStateRoutingTests.swift"
      provides: "AppState top-level route resolution tests"
  key_links:
    - from: "ios/dAIlyTests/DeepLinkRoutingTests.swift"
      to: "ios/dAIly/dAIlyApp.swift onOpenURL handler"
      via: "URL parsing → AppState transitions"
      pattern: "OAuthCallbackParser|PairCodeURLParser"
---

<objective>
iOS already has unit tests for the leaf state machines (VoiceSession, OAuthCallbackParser, AuthService, IntegrationState, AppState basics, KeychainStore). What's missing per the phase goal: **onboarding flow** (multi-step transitions across the TabView), **deep-link routing** (oauth-callback and pair URLs hitting `onOpenURL`), and explicit **AppState routing** tests proving the user lands on the right screen for each auth/onboarding state combination.

Purpose: These are the exact surfaces that broke during Phase 21.2 bug-fixing. Lock them down before TestFlight.
Output: Three new XCTest files in ios/dAIlyTests/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@ios/dAIly/AppState.swift
@ios/dAIly/dAIlyApp.swift
@ios/dAIly/onboarding/OnboardingView.swift
@ios/dAIly/onboarding/IntegrationState.swift
@ios/dAIly/onboarding/IntegrationProvider.swift
@ios/dAIly/auth/OAuthCallbackParser.swift
@ios/dAIly/auth/PairCodeURLParser.swift
@ios/dAIlyTests/AppStateTests.swift
@ios/dAIlyTests/IntegrationStateTests.swift
@ios/dAIlyTests/OAuthCallbackParserTests.swift
@ios/dAIlyTests/PairCodeURLParserTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OnboardingFlowTests — multi-step state transitions</name>
  <files>ios/dAIlyTests/OnboardingFlowTests.swift</files>
  <read_first>
    - ios/dAIly/onboarding/OnboardingView.swift (find the step enum, advance/back functions)
    - ios/dAIly/onboarding/IntegrationState.swift
    - ios/dAIly/onboarding/IntegrationProvider.swift
    - ios/dAIly/AppState.swift (onboarding-related properties)
    - ios/dAIlyTests/IntegrationStateTests.swift (existing pattern — match style)
  </read_first>
  <behavior>
    Tests for the onboarding step machine inside OnboardingView (or wherever the step enum lives — likely AppState or OnboardingView). Cases:
    - test_onboarding_starts_at_welcome: fresh AppState → currentOnboardingStep == .welcome
    - test_advance_from_welcome_goes_to_integrations: advance() → .integrations
    - test_integrations_step_requires_at_least_one_connected: with all IntegrationState == .disconnected, canAdvance == false; with google == .connected, canAdvance == true
    - test_advance_from_integrations_goes_to_schedule
    - test_advance_from_schedule_goes_to_permissions (or directly completion if permissions step is conditional)
    - test_advance_from_permissions_goes_to_completion
    - test_advance_from_completion_marks_onboarding_done: completion → AppState.hasCompletedOnboarding == true
    - test_back_from_schedule_returns_to_integrations
    - test_back_from_welcome_is_noop
    - test_oauth_callback_updates_integration_state_on_current_step: simulate /oauth/success?provider=google&status=connected → integrationStates[.google] == .connected
    If the step enum doesn't exist as a testable type, expose it via a small refactor in OnboardingView (move enum out of view body to file scope) — but only if needed; prefer testing existing public surface.
  </behavior>
  <action>
    Create ios/dAIlyTests/OnboardingFlowTests.swift. Use XCTest. Mirror the fixture/setup style of IntegrationStateTests.swift. Instantiate AppState directly (no SwiftUI rendering — just state machine). For each test, drive the step transitions through the public methods on AppState/OnboardingView model. Do NOT use ViewInspector or render SwiftUI views; pure model tests only. If a method needed for testing is currently private in OnboardingView, change it to internal (Swift default) or move logic into AppState — keep this refactor minimal.
  </action>
  <verify>
    <automated>cd ios && xcodebuild test -project dAIly.xcodeproj -scheme dAIly -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:dAIlyTests/OnboardingFlowTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:dAIlyTests/OnboardingFlowTests` reports "** TEST SUCCEEDED **"
    - `grep -c "func test" ios/dAIlyTests/OnboardingFlowTests.swift` returns 9 or more
    - File compiles with no warnings about unused variables in test bodies
  </acceptance_criteria>
  <done>9+ onboarding flow tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeepLinkRoutingTests — URL handler routing</name>
  <files>ios/dAIlyTests/DeepLinkRoutingTests.swift</files>
  <read_first>
    - ios/dAIly/dAIlyApp.swift (find .onOpenURL { url in ... } and the handler function)
    - ios/dAIly/auth/OAuthCallbackParser.swift
    - ios/dAIly/auth/PairCodeURLParser.swift
    - ios/dAIly/AppState.swift (handleDeepLink or equivalent)
    - ios/dAIlyTests/OAuthCallbackParserTests.swift
    - ios/dAIlyTests/PairCodeURLParserTests.swift
  </read_first>
  <behavior>
    Tests verifying that incoming URLs route to the correct AppState mutation (NOT just that the parsers work — that's already covered). Cases:
    - test_oauth_success_url_marks_provider_connected: feed URL "daily://oauth/success?provider=google&status=connected" into AppState.handleDeepLink → integrationStates[.google] == .connected
    - test_oauth_success_unknown_provider_is_ignored: provider=unknown → no state change, no crash
    - test_oauth_failure_url_marks_provider_failed: status=error → integrationStates[.google] == .failed (or surfaces error)
    - test_pair_code_url_initiates_pairing: feed "https://daily.app/pair?code=123456" → AppState.pairingCode == "123456" and pair() coroutine triggered (or queued)
    - test_universal_link_with_invalid_path_is_ignored: "https://daily.app/garbage" → no state change
    - test_oauth_callback_updates_integration_then_advances_step_if_in_onboarding: while currentOnboardingStep == .integrations, OAuth success for google → state connected AND step still .integrations (advance is user-driven, not auto)
    - test_deeplink_during_voice_session_does_not_disconnect: while AppState.voiceSession is .connected, oauth deeplink arrives → voice session stays .connected
    Use a fake/mocked AuthService where the handler calls into network — verify the correct method is invoked rather than executing real HTTP.
  </behavior>
  <action>
    Create ios/dAIlyTests/DeepLinkRoutingTests.swift. Identify the deep-link handler in dAIlyApp.swift (likely a function like `handleDeepLink(_:)` on AppState or a free function). If it's inline inside .onOpenURL, refactor to a method on AppState first (smallest possible change). Inject a mock AuthService where required. Use synchronous assertions where the handler is sync; for async paths, use `await` with the new structured concurrency XCTest support.
  </action>
  <verify>
    <automated>cd ios && xcodebuild test -project dAIly.xcodeproj -scheme dAIly -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:dAIlyTests/DeepLinkRoutingTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:dAIlyTests/DeepLinkRoutingTests` reports "** TEST SUCCEEDED **"
    - `grep -c "func test" ios/dAIlyTests/DeepLinkRoutingTests.swift` returns 7 or more
    - File references both OAuthCallbackParser and PairCodeURLParser (`grep -E "OAuthCallbackParser|PairCodeURLParser" ios/dAIlyTests/DeepLinkRoutingTests.swift` returns 2+ matches)
  </acceptance_criteria>
  <done>7+ deep-link routing tests passing; handler logic exercised end-to-end with mocked AuthService.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AppStateRoutingTests — top-level screen resolution</name>
  <files>ios/dAIlyTests/AppStateRoutingTests.swift</files>
  <read_first>
    - ios/dAIly/AppState.swift (look for currentRoute, rootView, or a computed property that decides which screen to show)
    - ios/dAIly/dAIlyApp.swift (the body's switch/if for which view to show)
    - ios/dAIlyTests/AppStateTests.swift (existing — do NOT duplicate)
  </read_first>
  <behavior>
    Tests for the computed property / switch that decides which root screen renders. Likely combinations:
    - test_no_token_routes_to_pairing: AppState with no access token → rootRoute == .pairing
    - test_token_but_onboarding_incomplete_routes_to_onboarding: has token, hasCompletedOnboarding == false → .onboarding
    - test_token_and_onboarding_complete_routes_to_voice: has token, hasCompletedOnboarding == true → .voice
    - test_signed_out_clears_route_to_pairing: from .voice, call signOut() → .pairing (and tokens cleared)
    - test_token_refresh_failure_routes_to_pairing: simulate 401 on token refresh → state becomes signed-out → .pairing
    - test_completing_onboarding_advances_route_to_voice: from .onboarding, mark completion → .voice
    If AppState doesn't expose a rootRoute enum, this task creates one (small refactor) — extract the inline conditional in dAIlyApp.swift body into a `var rootRoute: RootRoute` on AppState. This makes the routing testable AND the dAIlyApp.swift body cleaner.
  </behavior>
  <action>
    Create ios/dAIlyTests/AppStateRoutingTests.swift. If a `rootRoute` computed property does not exist on AppState, add it as part of this task with a `RootRoute` enum (.pairing, .onboarding, .voice, .signedOut). Update dAIlyApp.swift to use `appState.rootRoute` in its body. Then write the 6 tests above. Each test sets up AppState with specific values and asserts the route enum.
  </action>
  <verify>
    <automated>cd ios && xcodebuild test -project dAIly.xcodeproj -scheme dAIly -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:dAIlyTests/AppStateRoutingTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:dAIlyTests/AppStateRoutingTests` reports "** TEST SUCCEEDED **"
    - `grep -c "func test" ios/dAIlyTests/AppStateRoutingTests.swift` returns 6 or more
    - `grep "rootRoute" ios/dAIly/AppState.swift` returns at least one match (property exists)
    - `xcodebuild test ... -only-testing:dAIlyTests` (full iOS suite) reports "** TEST SUCCEEDED **" — no regression from refactor
  </acceptance_criteria>
  <done>6+ routing tests passing; AppState.rootRoute exposed as a tested public surface; full iOS test suite still green.</done>
</task>

</tasks>

<verification>
- Three new test files compile and pass on iPhone 15 Simulator
- 22+ new XCTest functions across the three files
- Full dAIlyTests target still passes after any refactors
</verification>

<success_criteria>
- `xcodebuild test ... -only-testing:dAIlyTests` reports "** TEST SUCCEEDED **"
- Onboarding flow, deep-link routing, and AppState routing all unit-tested
- AppState gains a testable rootRoute surface
</success_criteria>

<output>
After completion, create `.planning/phases/21.4-test-coverage/21.4-02-SUMMARY.md` listing files added, test counts, and any AppState refactor notes.
</output>
