---
phase: 16-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-observability/14-VALIDATION.md
  - .planning/phases/15-deployment/15-VALIDATION.md
  - src/daily/profile/adaptive_ranker.py
  - src/daily/orchestrator/nodes.py
  - src/daily/voice/loop.py
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Phase 14 VALIDATION.md shows nyquist_compliant: true and status: compliant"
    - "Phase 15 VALIDATION.md shows nyquist_compliant: true and status: compliant"
    - "adaptive_ranker.py uses make_logger with stage=ranker instead of logging.getLogger"
    - "nodes.py uses make_logger with stage=orchestrator instead of logging.getLogger"
    - "voice/loop.py uses make_logger with stage=voice instead of logging.getLogger"
    - "All existing tests still pass after make_logger adoption"
  artifacts:
    - path: ".planning/phases/14-observability/14-VALIDATION.md"
      provides: "Updated validation status"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/15-deployment/15-VALIDATION.md"
      provides: "Updated validation status"
      contains: "nyquist_compliant: true"
    - path: "src/daily/profile/adaptive_ranker.py"
      provides: "Structured logging with stage context"
      contains: "make_logger"
    - path: "src/daily/orchestrator/nodes.py"
      provides: "Structured logging with stage context"
      contains: "make_logger"
    - path: "src/daily/voice/loop.py"
      provides: "Structured logging with stage context"
      contains: "make_logger"
  key_links:
    - from: "src/daily/profile/adaptive_ranker.py"
      to: "src/daily/logging_config.py"
      via: "from daily.logging_config import make_logger"
      pattern: "from daily\\.logging_config import make_logger"
    - from: "src/daily/orchestrator/nodes.py"
      to: "src/daily/logging_config.py"
      via: "from daily.logging_config import make_logger"
      pattern: "from daily\\.logging_config import make_logger"
    - from: "src/daily/voice/loop.py"
      to: "src/daily/logging_config.py"
      via: "from daily.logging_config import make_logger"
      pattern: "from daily\\.logging_config import make_logger"
---

<objective>
Close all v1.2 tech debt identified in the milestone audit before archiving.

Purpose: The audit found 3 remaining items: two VALIDATION.md files stuck in draft status despite
all tests passing, and three Phase 13 modules using bare logging.getLogger instead of the
make_logger factory from Phase 14. Closing these ensures the milestone is clean.

Output: Updated VALIDATION.md files (compliant status), three source files using make_logger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md

<interfaces>
<!-- make_logger API from src/daily/logging_config.py -->
```python
def make_logger(name: str, **ctx_fields: Any) -> logging.LoggerAdapter:
    """Return a ContextAdapter that injects ctx_fields into every log record.

    Args:
        name: Logger name, typically __name__ of the calling module.
        **ctx_fields: Context fields to inject (e.g., user_id=1, stage="precompute").

    Returns:
        ContextAdapter wrapping the named logger.
    """
    return ContextAdapter(logging.getLogger(name), extra={"ctx": ctx_fields})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Phase 14 and Phase 15 VALIDATION.md to compliant</name>
  <files>
    .planning/phases/14-observability/14-VALIDATION.md
    .planning/phases/15-deployment/15-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/14-observability/14-VALIDATION.md
    .planning/phases/15-deployment/15-VALIDATION.md
    .planning/v1.2-MILESTONE-AUDIT.md
  </read_first>
  <action>
Update the YAML frontmatter in both VALIDATION.md files:

**Phase 14 (.planning/phases/14-observability/14-VALIDATION.md):**
- Change `status: draft` to `status: compliant`
- Change `nyquist_compliant: false` to `nyquist_compliant: true`
- Change `wave_0_complete: false` to `wave_0_complete: true`
- In the "Validation Sign-Off" section at the bottom, tick all checkboxes `[x]`
- Change `**Approval:** pending` to `**Approval:** approved (2026-04-19 — all 14 tests passing)`

**Phase 15 (.planning/phases/15-deployment/15-VALIDATION.md):**
- Change `status: draft` to `status: compliant`
- Change `nyquist_compliant: false` to `nyquist_compliant: true`
- Change `wave_0_complete: false` to `wave_0_complete: true`
- In the "Validation Sign-Off" section at the bottom, tick all checkboxes `[x]`
- Change `**Approval:** pending` to `**Approval:** approved (2026-04-19 — human checkpoint passed, infra verified)`

No other content changes. Leave the rest of each file intact.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/14-observability/14-VALIDATION.md .planning/phases/15-deployment/15-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "nyquist_compliant: true" on 14-VALIDATION.md returns a match
    - grep "nyquist_compliant: true" on 15-VALIDATION.md returns a match
    - grep "wave_0_complete: true" on both files returns a match
    - grep "status: compliant" on both files returns a match
    - grep "Approval.*approved" on both files returns a match
  </acceptance_criteria>
  <done>Both VALIDATION.md files show nyquist_compliant: true, wave_0_complete: true, status: compliant, and approval: approved</done>
</task>

<task type="auto">
  <name>Task 2: Adopt make_logger in adaptive_ranker.py, nodes.py, and voice/loop.py</name>
  <files>
    src/daily/profile/adaptive_ranker.py
    src/daily/orchestrator/nodes.py
    src/daily/voice/loop.py
  </files>
  <read_first>
    src/daily/logging_config.py
    src/daily/profile/adaptive_ranker.py
    src/daily/orchestrator/nodes.py
    src/daily/voice/loop.py
  </read_first>
  <action>
In each of the three files, make two changes:

**1. src/daily/profile/adaptive_ranker.py:**
- Replace `import logging` (line 13) with `from daily.logging_config import make_logger`
- Replace `logger = logging.getLogger(__name__)` (line 22) with `logger = make_logger(__name__, stage="ranker")`
- Remove `from __future__ import annotations` ONLY if `logging` was its sole reason (it is not — keep it, it is used for type annotations)
- Verify: `logging` is no longer imported. `make_logger` is imported from `daily.logging_config`.

**2. src/daily/orchestrator/nodes.py:**
- Replace `import logging` (line 24) with nothing (remove the line)
- Add `from daily.logging_config import make_logger` after the existing imports from daily.* (around line 40, after the other `from daily...` imports)
- Replace `logger = logging.getLogger(__name__)` (line 41) with `logger = make_logger(__name__, stage="orchestrator")`
- IMPORTANT: nodes.py also uses `logging.getLogger` indirectly? No — check all usages. The only usage is the module-level `logger = logging.getLogger(__name__)`. The `import logging` line can be removed entirely since no other reference to `logging.*` exists in the file.

**3. src/daily/voice/loop.py:**
- Replace `import logging` (line 17) with nothing (remove the line)
- Add `from daily.logging_config import make_logger` after the existing `from daily.config import Settings` import
- Replace `logger = logging.getLogger(__name__)` (line 38) with `logger = make_logger(__name__, stage="voice")`
- Verify: no other references to `logging.*` remain in the file.

All three files use `logger.info(...)`, `logger.warning(...)`, `logger.error(...)` etc. These work identically on the LoggerAdapter returned by make_logger — no call-site changes needed.
  </action>
  <verify>
    <automated>cd /Users/jacobmarriott/Documents/Personal/projects/dAIly && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "make_logger" src/daily/profile/adaptive_ranker.py returns a match
    - grep "make_logger" src/daily/orchestrator/nodes.py returns a match
    - grep "make_logger" src/daily/voice/loop.py returns a match
    - grep "logging.getLogger" across all three files returns zero matches
    - grep 'stage="ranker"' src/daily/profile/adaptive_ranker.py returns a match
    - grep 'stage="orchestrator"' src/daily/orchestrator/nodes.py returns a match
    - grep 'stage="voice"' src/daily/voice/loop.py returns a match
    - pytest tests/ -x passes with no failures
  </acceptance_criteria>
  <done>All three files import make_logger from daily.logging_config, use it with appropriate stage context, and the full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is a mechanical refactor of logging imports and
documentation updates. No user input, no API changes, no credential handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | I (Info Disclosure) | make_logger ctx fields | accept | ctx contains only stage name (string literal) — no PII, no secrets. Same as existing Phase 14 modules. |
</threat_model>

<verification>
1. `grep -r "logging.getLogger" src/daily/profile/adaptive_ranker.py src/daily/orchestrator/nodes.py src/daily/voice/loop.py` returns no matches
2. `grep -r "make_logger" src/daily/profile/adaptive_ranker.py src/daily/orchestrator/nodes.py src/daily/voice/loop.py` returns 3 matches (one per file)
3. `grep "nyquist_compliant: true" .planning/phases/14-observability/14-VALIDATION.md .planning/phases/15-deployment/15-VALIDATION.md` returns 2 matches
4. `pytest tests/ -x` passes with no failures
</verification>

<success_criteria>
- Phase 14 and Phase 15 VALIDATION.md files are marked compliant with nyquist_compliant: true
- All three Phase 13 hot-path modules (adaptive_ranker.py, nodes.py, voice/loop.py) use make_logger with stage context
- No bare logging.getLogger remains in those three files
- Full test suite passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/16-milestone-closeout/16-01-SUMMARY.md`
</output>
