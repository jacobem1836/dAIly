---
phase: 21
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_integrations_router.py
  - tests/test_users_router.py
  - tests/test_aasa.py
  - tests/conftest.py
autonomous: true
requirements:
  - WAVE0-TESTS
user_setup: []
must_haves:
  truths:
    - "Test scaffold exists for every test referenced in 21-VALIDATION.md"
    - "Tests fail with clear MISSING-IMPLEMENTATION error (not import error)"
    - "Shared fixtures (test_user, mock_redis, async_db_session) are reusable"
  artifacts:
    - path: "tests/test_integrations_router.py"
      provides: "Test stubs for INT-01 through INT-07"
      min_lines: 60
    - path: "tests/test_users_router.py"
      provides: "Test stubs for USR-01 through USR-03"
      min_lines: 40
  key_links:
    - from: "tests/test_integrations_router.py"
      to: "src/daily/integrations/router.py (Plan 03)"
      via: "imports `from daily.integrations.router import router`"
      pattern: "from daily.integrations.router"
    - from: "tests/test_users_router.py"
      to: "src/daily/users/router.py (Plan 04)"
      via: "imports `from daily.users.router import router`"
      pattern: "from daily.users.router"
---

<objective>
Create the Wave 0 test scaffold required by `21-VALIDATION.md`. Every test referenced in the validation map must exist and be runnable (even if currently failing) so downstream plans (03, 04, 05) can satisfy `<verify>` blocks.

Purpose: Per Nyquist sampling rule, every executor task must point at an `<automated>` command. The implementation files don't exist yet, so the tests must be written first (RED state).
Output: Four test files containing all tests listed in `21-RESEARCH.md` "Phase Requirements → Test Map".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-per-user-onboarding/21-RESEARCH.md
@.planning/phases/21-per-user-onboarding/21-VALIDATION.md
@tests/conftest.py
@tests/test_aasa.py
@tests/test_auth_send_link.py
@tests/test_auth_pairing.py

<interfaces>
Patterns to mirror (read these first for fixture usage):
- `tests/conftest.py` — existing shared fixtures
- `tests/test_auth_send_link.py` — async FastAPI TestClient pattern with mocked Resend
- `tests/test_auth_pairing.py` — async DB session fixture pattern
- `tests/test_livekit_token.py` — auth Bearer token in tests

Tests must import the (not-yet-existing) modules:
- `from daily.integrations.router import router as integrations_router` (created in Plan 03)
- `from daily.users.router import router as users_router` (created in Plan 04)

Mark each test with `@pytest.mark.skip(reason="MISSING — implemented in Plan 03/04")` until the implementation exists, OR write the test body so it imports inside the test (raising ImportError that pytest reports as expected failure). Use the SKIP approach.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/test_integrations_router.py with 7 stub tests</name>
  <files>tests/test_integrations_router.py</files>
  <read_first>
    - tests/conftest.py (existing fixtures)
    - tests/test_auth_send_link.py (TestClient + httpx_mock pattern)
    - tests/test_livekit_token.py (auth header pattern)
    - .planning/phases/21-per-user-onboarding/21-RESEARCH.md (Phase Requirements → Test Map section)
  </read_first>
  <action>
    Create `tests/test_integrations_router.py` containing the following 7 test stubs. Each test must be marked with `@pytest.mark.skip(reason="MISSING — implemented in Plan 03")` and contain a docstring describing what it WILL verify. Test names must match exactly:

    1. `test_google_connect` — INT-01: `GET /integrations/google/connect` with valid Bearer token returns 200 with `auth_url` field, and Redis key `oauth_state:{state}` is set with TTL ≤ 600s.
    2. `test_google_callback` — INT-02: `GET /integrations/google/callback?code=X&state=Y` with valid state in Redis exchanges code, encrypts and stores token (provider="google"), and returns 302 redirect to `/oauth/success?provider=google`.
    3. `test_microsoft_connect` — INT-03: `GET /integrations/microsoft/connect` returns 200 with `auth_url`.
    4. `test_microsoft_callback` — INT-04: callback exchanges code via msal mock, stores IntegrationToken with `provider="outlook"` (NOT "microsoft").
    5. `test_slack_connect` — INT-05: `GET /integrations/slack/connect` returns 200 with `auth_url`.
    6. `test_slack_callback` — INT-06: callback uses async httpx to POST to `oauth.v2.access`, stores token with `provider="slack"`.
    7. `test_invalid_state` — INT-07: callback with state not in Redis returns HTTP 400.
    8. `test_connect_requires_auth` — `GET /integrations/google/connect` without Bearer token returns 401.

    Each test body should be a one-line `pytest.skip("Plan 03 — pending implementation")` or `assert False, "Plan 03 — pending"` after the skip mark. Include the imports at the top:

    ```python
    import pytest
    from httpx import AsyncClient
    # The following will fail until Plan 03 creates the module — imports are
    # inside test functions so pytest collection succeeds.
    ```

    Do NOT import `daily.integrations.router` at module level — import inside the test body so collection works before Plan 03 lands.
  </action>
  <verify>
    <automated>pytest tests/test_integrations_router.py --collect-only -q 2>&1 | grep -E "test_(google_connect|google_callback|microsoft_connect|microsoft_callback|slack_connect|slack_callback|invalid_state|connect_requires_auth)" | wc -l | grep -q "8"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_integrations_router.py` exists
    - `pytest tests/test_integrations_router.py --collect-only` lists exactly 8 test functions
    - Test names exactly: `test_google_connect`, `test_google_callback`, `test_microsoft_connect`, `test_microsoft_callback`, `test_slack_connect`, `test_slack_callback`, `test_invalid_state`, `test_connect_requires_auth`
    - Each test has `@pytest.mark.skip` decorator referencing Plan 03
    - `pytest tests/test_integrations_router.py -q` reports 8 skipped, 0 failed, 0 errors
  </acceptance_criteria>
  <done>All 8 stub tests collected and reported skipped (not errored).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/test_users_router.py with 3 stub tests</name>
  <files>tests/test_users_router.py</files>
  <read_first>
    - tests/conftest.py
    - tests/test_livekit_token.py (auth pattern)
    - .planning/phases/21-per-user-onboarding/21-RESEARCH.md (USR-01..03 rows)
  </read_first>
  <action>
    Create `tests/test_users_router.py` containing 3 test stubs, all marked `@pytest.mark.skip(reason="MISSING — implemented in Plan 04")`. Test names exactly:

    1. `test_integration_status` — USR-01: `GET /users/me/integrations` with auth returns `{google: bool, microsoft: bool, slack: bool}` based on `IntegrationToken` rows for the current user (provider="outlook" maps to `microsoft: true`).
    2. `test_update_preferences` — USR-02: `PUT /users/me/preferences` with `{briefing_time: "07:00", timezone: "Australia/Brisbane"}` upserts `BriefingConfig` row and stores UTC-converted `schedule_hour`/`schedule_minute` plus the IANA `timezone` string.
    3. `test_invalid_timezone` — USR-03: `PUT /users/me/preferences` with `timezone="Not/A_Zone"` returns HTTP 422.

    Same import-inside-test pattern as Plan 03 tests.
  </action>
  <verify>
    <automated>pytest tests/test_users_router.py --collect-only -q 2>&1 | grep -E "test_(integration_status|update_preferences|invalid_timezone)" | wc -l | grep -q "3"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_users_router.py` exists
    - Exactly 3 test functions collected: `test_integration_status`, `test_update_preferences`, `test_invalid_timezone`
    - Each test has `@pytest.mark.skip` decorator referencing Plan 04
    - `pytest tests/test_users_router.py -q` reports 3 skipped
  </acceptance_criteria>
  <done>All 3 stub tests collected and reported skipped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend tests/test_aasa.py to assert /oauth/success path</name>
  <files>tests/test_aasa.py</files>
  <read_first>
    - tests/test_aasa.py (full existing file)
    - src/daily/main.py lines 106-128 (current AASA handler)
  </read_first>
  <action>
    Add a new test function `test_aasa_includes_oauth_success_path` to `tests/test_aasa.py`. It must call the AASA endpoint and assert that `"/oauth/success"` is in the `applinks.details[0].paths` list.

    Mark with `@pytest.mark.skip(reason="MISSING — Plan 05 adds /oauth/success to AASA paths")` so it does not break the build until Plan 05 lands.

    Test body (skeleton):
    ```python
    @pytest.mark.skip(reason="Plan 05 — adds /oauth/success to AASA paths")
    def test_aasa_includes_oauth_success_path(client):
        resp = client.get("/.well-known/apple-app-site-association")
        assert resp.status_code == 200
        paths = resp.json()["applinks"]["details"][0]["paths"]
        assert "/oauth/success" in paths
    ```

    Reuse the existing `client` fixture if `tests/test_aasa.py` already defines one; otherwise import `TestClient` and instantiate as the existing tests in the file do. Do NOT modify or remove existing tests in this file.
  </action>
  <verify>
    <automated>grep -q "test_aasa_includes_oauth_success_path" tests/test_aasa.py && pytest tests/test_aasa.py -q 2>&1 | grep -E "passed|skipped"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "test_aasa_includes_oauth_success_path" tests/test_aasa.py` matches
    - The new test has `@pytest.mark.skip` decorator referencing Plan 05
    - `pytest tests/test_aasa.py -q` does not report new failures (existing tests still pass; new test skipped)
  </acceptance_criteria>
  <done>tests/test_aasa.py contains the new test, marked skipped, with the assertion body referencing `/oauth/success`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add shared fixtures to tests/conftest.py if missing</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (entire file — DO NOT duplicate fixtures already present)
    - tests/test_auth_pairing.py (DB session fixture pattern)
  </read_first>
  <action>
    Inspect `tests/conftest.py` for these fixtures and add only the ones that DO NOT already exist:

    1. `mock_redis` — async fixture returning a `redis.asyncio.Redis` instance pointed at a fakeredis or test Redis. If `fakeredis.aioredis` is available, use `fakeredis.aioredis.FakeRedis()`. Otherwise, mark this fixture body as `pytest.skip("fakeredis not installed")` so Plan 03 implementer can wire the actual fake client.

    2. `auth_headers` — fixture returning `{"Authorization": "Bearer <token>"}` for a test user. Use `daily.auth.jwt.encode_access_token(user_id, settings)` to build the token. Reuse the test user fixture if present; otherwise skip with a clear message.

    Do NOT redefine fixtures that already exist (e.g., `client`, `db_session`). Only add what's missing. If both fixtures already exist, this task is a no-op — leave the file untouched and write a comment to the SUMMARY noting "no changes needed".

    Append (not prepend) to the end of conftest.py.
  </action>
  <verify>
    <automated>(grep -q "mock_redis" tests/conftest.py && grep -q "auth_headers" tests/conftest.py) && pytest tests/conftest.py --collect-only -q 2>&1 | grep -v error</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines `mock_redis` fixture (either pre-existing or newly added)
    - `tests/conftest.py` defines `auth_headers` fixture (either pre-existing or newly added)
    - `pytest tests/ --collect-only -q` exits 0 (no collection errors introduced)
  </acceptance_criteria>
  <done>Both fixtures available for downstream plan tests. No regression in collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → app code | Test fixtures import production modules; importable surface area |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Tampering | conftest.py | mitigate | Append-only changes; do not modify existing fixtures used by other phases' tests |
| T-21-02-02 | Information Disclosure | test fixtures | accept | Tests use fake credentials; no real secrets touched |
</threat_model>

<verification>
- `pytest tests/test_integrations_router.py tests/test_users_router.py tests/test_aasa.py --collect-only -q` exits 0
- 8 + 3 + (existing + 1) tests collected; new ones marked skipped
- `pytest tests/ -q` reports same pass count as before, plus N new skipped
</verification>

<success_criteria>
- All Wave 0 test files exist and collect without import errors
- Downstream Plans 03, 04, 05 can `@pytest.mark.skip` → unskip as they implement
</success_criteria>

<output>
After completion, create `.planning/phases/21-per-user-onboarding/21-02-SUMMARY.md`.
</output>
