---
phase: 17-voice-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/daily/voice/tts.py
  - tests/test_voice_tts.py
autonomous: true
requirements: [VOICE-POLISH-03]
must_haves:
  truths:
    - "When stop_event is set mid-stream, the current audio chunk completes playback before TTS exits"
    - "TTS no longer cuts off mid-word on barge-in"
  artifacts:
    - path: "src/daily/voice/tts.py"
      provides: "play_streaming with stop_event check after output_stream.write"
      contains: "if stop_event.is_set():"
  key_links:
    - from: "tts.py play_streaming receive loop"
      to: "stop_event check ordering"
      via: "stop_event check moved AFTER output_stream.write"
      pattern: "output_stream.write.*\\n.*if stop_event.is_set"
---

<objective>
Implement Improvement 3 (graceful TTS fade-out): the `stop_event.is_set()` check in
`TTSPipeline.play_streaming()` must occur AFTER `output_stream.write(response.audio)`,
not before. This lets the current audio chunk finish playing before TTS exits, so
barge-ins do not cut off mid-word.

Purpose: First step of voice polish — minimal one-line reorder. Standalone, no
dependencies on other Phase 17 plans.
Output: tts.py reordered, impacted test updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-voice-polish/17-CONTEXT.md
@.planning/phases/17-voice-polish/17-RESEARCH.md
@src/daily/voice/tts.py
@tests/test_voice_tts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder stop_event check in play_streaming receive loop</name>
  <files>src/daily/voice/tts.py</files>
  <read_first>
    - src/daily/voice/tts.py (full file — must see current receive loop at lines 161-167)
    - .planning/phases/17-voice-polish/17-CONTEXT.md (Improvement 3 spec)
  </read_first>
  <action>
    In `src/daily/voice/tts.py`, locate the `async for response in ctx.receive():`
    block in `play_streaming` (currently around lines 161-167). Current state:

    ```python
    async for response in ctx.receive():
        if stop_event.is_set():
            break  # Barge-in detected — stop immediately (D-04)
        if response.type == "chunk" and response.audio:
            output_stream.write(response.audio)
    ```

    Replace with this exact ordering (write FIRST, check stop_event AFTER):

    ```python
    async for response in ctx.receive():
        if response.type == "chunk" and response.audio:
            output_stream.write(response.audio)
        if stop_event.is_set():
            break  # Barge-in detected — finish current chunk, then stop (graceful fade-out)
    ```

    Per D-03 (CONTEXT.md Improvement 3): the current chunk must complete before exit.
    Do NOT drain the full sentence buffer — only the chunk that was already received
    is written.

    Do not change any other logic in `play_streaming`. Do not modify `play_streaming_tokens`
    or any other method (those are added in Plan 04).
  </action>
  <verify>
    <automated>grep -nA1 "output_stream.write(response.audio)" src/daily/voice/tts.py | grep -A1 "output_stream.write" | grep "if stop_event.is_set"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "output_stream.write(response.audio)" src/daily/voice/tts.py` shows the write line.
    - The line immediately after the write (within the same `async for` block) is `if stop_event.is_set():`.
    - `python -c "import ast; ast.parse(open('src/daily/voice/tts.py').read())"` exits 0 (file parses).
    - No other code in tts.py changed (verified by `git diff src/daily/voice/tts.py` showing only the reorder hunk).
  </acceptance_criteria>
  <done>
    Stop event check is AFTER output_stream.write inside the receive loop. File parses.
    No unrelated changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test_play_streaming_stops_on_event for graceful fade-out</name>
  <files>tests/test_voice_tts.py</files>
  <read_first>
    - tests/test_voice_tts.py (full file — find `test_play_streaming_stops_on_event`)
    - src/daily/voice/tts.py (to confirm new behavior from Task 1)
  </read_first>
  <action>
    Locate `test_play_streaming_stops_on_event` in `tests/test_voice_tts.py`. The test
    currently sets `stop_event` at index 2 and asserts `len(chunks_written) < 5`.

    After the reorder in Task 1, the chunk at index 2 WILL be written before the break
    fires. Update the test so it asserts the new graceful semantics:

    1. Keep the trigger of `stop_event.set()` at the same chunk index it currently uses.
    2. Change the assertion to `assert len(chunks_written) == <expected_count>` where
       `expected_count` equals (index_at_which_stop_was_set + 1) — i.e. the chunk that
       was being written when stop_event was set IS included in chunks_written, and
       no subsequent chunks are.
    3. Add an inline comment: `# Graceful fade-out: current chunk completes before break (Improvement 3)`.

    If the trigger is at index 2, the assertion becomes `assert len(chunks_written) == 3`.
    Read the test source first to confirm the exact trigger index used.

    Do not modify any other test in this file.
  </action>
  <verify>
    <automated>pytest tests/test_voice_tts.py::test_play_streaming_stops_on_event -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_voice_tts.py::test_play_streaming_stops_on_event -x` passes.
    - `pytest tests/test_voice_tts.py -x` passes (no other test broken).
    - `grep -n "Graceful fade-out" tests/test_voice_tts.py` shows the new comment.
    - The assertion uses `==` against the trigger-index+1 value (verified by reading the test).
  </acceptance_criteria>
  <done>
    Test reflects new graceful fade-out semantics. Test passes. No other tests in the
    file regressed.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_voice_tts.py -x` passes.
2. `python -c "import ast; ast.parse(open('src/daily/voice/tts.py').read())"` exits 0.
3. `grep -nA2 "async for response in ctx.receive" src/daily/voice/tts.py` shows write
   before stop_event check.
</verification>

<success_criteria>
- Stop event check ordering reversed in `play_streaming`.
- `test_play_streaming_stops_on_event` updated and passing.
- No unrelated changes in tts.py or test file.
</success_criteria>

<output>
After completion, create `.planning/phases/17-voice-polish/17-01-SUMMARY.md`.
</output>
