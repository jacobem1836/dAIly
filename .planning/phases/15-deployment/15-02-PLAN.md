---
phase: 15-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "Every env var in config.py Settings class has a corresponding entry in .env.example"
    - ".env.example contains no real secrets -- only placeholders and generation instructions"
    - "Both local dev and Docker Compose hostname variants are documented with comments"
    - ".env is in .gitignore so real credentials cannot be accidentally committed"
  artifacts:
    - path: ".env.example"
      provides: "Complete environment variable template with descriptions and placeholders"
      contains: "REDIS_URL"
    - path: ".env.example"
      provides: "Docker vs local hostname documentation"
      contains: "Docker Compose:"
  key_links:
    - from: ".env.example"
      to: "src/daily/config.py"
      via: "Every Settings field has a matching env var entry"
      pattern: "DATABASE_URL|REDIS_URL|OPENAI_API_KEY|DEEPGRAM_API_KEY|CARTESIA_API_KEY|LOG_LEVEL|BRIEFING_EMAIL_TOP_N|BRIEFING_SCHEDULE_TIME|DATABASE_URL_PSYCOPG"
---

<objective>
Complete the .env.example file so every environment variable from `config.py` Settings class is documented with a description, placeholder value, and -- for connection strings -- both local dev and Docker Compose hostname variants.

Purpose: Satisfies DEPLOY-02 -- a developer can copy .env.example to .env, fill in their API keys, and have a working configuration for either local dev or Docker Compose.

Output: Updated .env.example with all 16 vars documented
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deployment/15-CONTEXT.md
@.planning/phases/15-deployment/15-RESEARCH.md

@src/daily/config.py
@.env.example
@.gitignore

<interfaces>
<!-- Source of truth: src/daily/config.py Settings class fields -->
<!-- Currently in .env.example (8 vars): DATABASE_URL, VAULT_KEY, GOOGLE_CLIENT_ID, -->
<!-- GOOGLE_CLIENT_SECRET, SLACK_CLIENT_ID, SLACK_CLIENT_SECRET, MICROSOFT_CLIENT_ID, -->
<!-- MICROSOFT_TENANT_ID -->
<!--  -->
<!-- Missing from .env.example (8 vars per D-07): -->
<!-- DATABASE_URL_PSYCOPG, REDIS_URL, OPENAI_API_KEY, DEEPGRAM_API_KEY, -->
<!-- CARTESIA_API_KEY, LOG_LEVEL, BRIEFING_EMAIL_TOP_N, BRIEFING_SCHEDULE_TIME -->
<!--  -->
<!-- .gitignore already contains .env (line 1) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete .env.example with all missing environment variables</name>
  <files>.env.example</files>
  <read_first>
    - src/daily/config.py (authoritative list of all Settings fields -- cross-check every field)
    - .env.example (current content -- 8 vars present, 8 missing)
    - .gitignore (confirm .env is listed)
    - .planning/phases/15-deployment/15-RESEARCH.md (exact template in "Code Examples" section)
  </read_first>
  <action>
Rewrite `.env.example` to be the complete template per D-07, D-08, D-09. Use the exact template from 15-RESEARCH.md "Code Examples" section as the reference, with these requirements:

1. **Group vars by category** with `# --- Category ---` comment headers:
   - Database (DATABASE_URL + DATABASE_URL_PSYCOPG)
   - Redis (REDIS_URL)
   - Encryption (VAULT_KEY)
   - OAuth: Google (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET)
   - OAuth: Slack (SLACK_CLIENT_ID, SLACK_CLIENT_SECRET)
   - OAuth: Microsoft (MICROSOFT_CLIENT_ID, MICROSOFT_TENANT_ID)
   - LLM (OPENAI_API_KEY)
   - Voice: STT (DEEPGRAM_API_KEY)
   - Voice: TTS (CARTESIA_API_KEY)
   - Briefing pipeline (BRIEFING_EMAIL_TOP_N, BRIEFING_SCHEDULE_TIME)
   - Observability (LOG_LEVEL)

2. **For connection strings** (DATABASE_URL, DATABASE_URL_PSYCOPG, REDIS_URL) per D-08:
   - Add a comment line showing the local dev value (localhost)
   - Add a comment line showing the Docker Compose value (service name)
   - Set the default value to the local dev variant (localhost)

3. **For VAULT_KEY** per D-09:
   - Keep the existing generation command as inline comment

4. **For API keys** (OPENAI, DEEPGRAM, CARTESIA, Google, Slack, Microsoft):
   - Empty value (no placeholder that could be mistaken for real)

5. **For defaults** (BRIEFING_EMAIL_TOP_N, BRIEFING_SCHEDULE_TIME, LOG_LEVEL):
   - Set sensible defaults matching the Settings class defaults (5, "05:00", "INFO")

Cross-check: After writing, verify every field in the Settings class from config.py has a matching line in .env.example. There should be exactly 16 env vars.
  </action>
  <verify>
    <automated>cd /Users/jacobmarriott/Documents/Personal/projects/dAIly && python3 -c "
import re
# Check .env.example has all required vars
with open('.env.example') as f:
    content = f.read()
required = ['DATABASE_URL', 'DATABASE_URL_PSYCOPG', 'VAULT_KEY', 'GOOGLE_CLIENT_ID',
    'GOOGLE_CLIENT_SECRET', 'SLACK_CLIENT_ID', 'SLACK_CLIENT_SECRET',
    'MICROSOFT_CLIENT_ID', 'MICROSOFT_TENANT_ID', 'REDIS_URL', 'OPENAI_API_KEY',
    'DEEPGRAM_API_KEY', 'CARTESIA_API_KEY', 'BRIEFING_EMAIL_TOP_N',
    'BRIEFING_SCHEDULE_TIME', 'LOG_LEVEL']
missing = [v for v in required if re.search(f'^{v}=', content, re.MULTILINE) is None]
if missing:
    print(f'FAIL: Missing vars: {missing}')
    exit(1)
# Check no real secrets
lines = [l for l in content.splitlines() if '=' in l and not l.strip().startswith('#')]
leaked = [l for l in lines if re.match(r'^[A-Z_]+=sk-|^[A-Z_]+=ghp_', l)]
if leaked:
    print(f'FAIL: Possible secrets: {leaked}')
    exit(1)
print(f'OK: All {len(required)} vars present, no secrets detected')
"</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains exactly 16 env var assignments (one per Settings field)
    - DATABASE_URL, DATABASE_URL_PSYCOPG, and REDIS_URL each have both local and Docker hostname comments
    - VAULT_KEY has the generation command as an inline comment
    - API key fields are empty (no placeholder values that look like real keys)
    - BRIEFING_EMAIL_TOP_N=5, BRIEFING_SCHEDULE_TIME=05:00, LOG_LEVEL=INFO are set as defaults
    - .gitignore contains `.env` (already confirmed -- no change needed)
    - The verification script exits 0 confirming all vars present and no secrets
  </acceptance_criteria>
  <done>.env.example is complete with all 16 env vars from config.py, grouped by category, with local/Docker hostname variants documented, and no real secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.example -> git | Template file committed to repo; must never contain real secrets |
| .env -> runtime | Real credentials file; must be in .gitignore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Information Disclosure | .env.example | mitigate | All API key fields are empty; verification script checks for leaked secret patterns (sk-, ghp_) |
| T-15-07 | Information Disclosure | .env committed to git | mitigate | .gitignore already contains .env (verified); task read_first confirms this |
| T-15-08 | Spoofing | Weak VAULT_KEY | mitigate | .env.example documents the cryptographically secure generation command (os.urandom(32) + base64) |
</threat_model>

<verification>
1. Verification script confirms all 16 vars present and no secrets detected
2. `grep -c '^[A-Z].*=' .env.example` returns 16
3. `grep '.env' .gitignore` confirms .env is gitignored
4. `grep 'Docker Compose:' .env.example` confirms Docker hostname variants are documented
</verification>

<success_criteria>
- .env.example documents every env var from config.py Settings class
- No secrets are committed in .env.example
- Both local dev and Docker Compose hostname variants are clearly documented
- A developer can copy .env.example to .env, fill in API keys, and start the stack
</success_criteria>

<output>
After completion, create `.planning/phases/15-deployment/15-02-SUMMARY.md`
</output>
