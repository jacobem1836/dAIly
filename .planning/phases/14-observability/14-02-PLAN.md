---
plan: 02
phase: 14-observability
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/daily/main.py
  - src/daily/briefing/pipeline.py
  - tests/test_health_endpoint.py
  - tests/test_metrics_endpoint.py
autonomous: true
requirements: [OBS-03, OBS-04]
must_haves:
  truths:
    - "GET /health returns 200 with status ok when DB, Redis, and scheduler are healthy"
    - "GET /health returns 503 with status degraded when any dependency is down"
    - "GET /metrics returns briefing_latency_avg_s, signals_7d, and memory_entries"
    - "Briefing pipeline writes latency_s key to Redis after each run"
    - "Signal counts in /metrics use a 7-day rolling window"
  artifacts:
    - path: "src/daily/main.py"
      provides: "Enhanced /health endpoint and new /metrics endpoint"
      contains: "/health"
    - path: "src/daily/briefing/pipeline.py"
      provides: "Latency timing and Redis key write"
      contains: "latency_s"
    - path: "tests/test_health_endpoint.py"
      provides: "Tests for /health 200 ok and 503 degraded paths"
    - path: "tests/test_metrics_endpoint.py"
      provides: "Tests for /metrics response shape and data sources"
  key_links:
    - from: "src/daily/main.py:/health"
      to: "DB (SELECT 1), Redis (PING), APScheduler (get_jobs)"
      via: "async probes in health endpoint"
      pattern: "SELECT 1"
    - from: "src/daily/main.py:/metrics"
      to: "signal_log table, memory_facts table, Redis briefing:*:latency_s keys"
      via: "async DB queries + Redis scan_iter"
      pattern: "scan_iter.*latency"
    - from: "src/daily/briefing/pipeline.py"
      to: "Redis briefing:{user_id}:latency_s"
      via: "redis.set after pipeline run"
      pattern: "latency_s"
---

# Plan 02: Health and Metrics Endpoints

<objective>
The system exposes its health and key metrics via two HTTP endpoints — /health for liveness probing and /metrics for operational insight — and the briefing pipeline records its latency in Redis for aggregation.

Purpose: Replace the stub /health with real dependency checks, add /metrics for operational visibility, and instrument the pipeline with latency recording.
Output: Enhanced /health, new /metrics, pipeline latency write, test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-observability/14-CONTEXT.md
@.planning/phases/14-observability/14-RESEARCH.md
@.planning/phases/14-observability/14-01-SUMMARY.md
@src/daily/main.py
@src/daily/briefing/pipeline.py
@src/daily/profile/signals.py
@src/daily/db/models.py
@src/daily/db/engine.py
@src/daily/config.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/daily/logging_config.py (created in Plan 01):
```python
def configure_logging(log_level: str = "INFO") -> None: ...
def make_logger(name: str, **ctx_fields) -> logging.LoggerAdapter: ...
```

From src/daily/db/engine.py:
```python
async_session: async_sessionmaker[AsyncSession]  # module-level session factory
```

From src/daily/profile/signals.py:
```python
class SignalLog(Base):
    __tablename__ = "signal_log"
    id: Mapped[int]
    user_id: Mapped[int]
    signal_type: Mapped[str]  # "skip", "expand", "re_request", etc.
    created_at: Mapped[datetime]
```

From src/daily/db/models.py:
```python
class MemoryFact(Base):
    __tablename__ = "memory_facts"
    id: Mapped[int]
```

From src/daily/briefing/scheduler.py:
```python
scheduler: AsyncIOScheduler  # module-level scheduler instance
# scheduler.get_jobs() returns list (synchronous, APScheduler 3.x)
```

From src/daily/config.py:
```python
class Settings(BaseSettings):
    redis_url: str = "redis://localhost:6379/0"
    log_level: str = "INFO"  # added in Plan 01
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /health endpoint | Open to any caller — returns dependency status |
| /metrics endpoint | Open to any caller — returns aggregate operational data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | /metrics | accept | Aggregate counts only (no PII). Acceptable for M1 single-host per CONTEXT.md. Gate behind auth in M2 if multi-tenant. |
| T-14-04 | Information Disclosure | /health | accept | Error messages from stdlib exceptions — no credentials. For production, restrict at nginx reverse proxy (Phase 15 scope). |
| T-14-05 | Denial of Service | /metrics Redis SCAN | accept | At M1 scale (1 user), scan matches 0-1 keys. Deferred: switch to Prometheus counters at 1k+ users. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace /health stub with real dependency checks and add /metrics endpoint</name>
  <files>src/daily/main.py</files>
  <read_first>
    - src/daily/main.py — current /health stub, lifespan, imports (modified by Plan 01)
    - src/daily/briefing/scheduler.py — scheduler instance, get_jobs() API
    - src/daily/db/engine.py — async_session factory
    - src/daily/profile/signals.py — SignalLog model, signal_type field, created_at
    - src/daily/db/models.py — MemoryFact model, tablename
    - src/daily/config.py — Settings class, redis_url
    - .planning/phases/14-observability/14-RESEARCH.md — Pattern 3 and Pattern 4, anti-patterns, Pitfall 5
    - .planning/phases/14-observability/14-CONTEXT.md — D-05 through D-12
  </read_first>
  <action>
    Modify `src/daily/main.py` to:

    1. **Replace `/health` endpoint** (per D-05, D-06, D-07):
       - Change signature to `async def health(response: Response) -> dict`
       - Import `Response` from `fastapi`
       - Probe DB: `async with async_session() as session: await session.execute(text("SELECT 1"))` — catch Exception, set `result["db"] = "error: {exc}"` and `degraded = True` on failure, `"ok"` on success
       - Probe Redis: create `AsyncRedis.from_url(Settings().redis_url)`, call `await redis.ping()`, close with `await redis.aclose()` in a `finally` block (per Pitfall 5). Set `result["redis"]` accordingly.
       - Check scheduler: `jobs = scheduler.get_jobs()` (synchronous, APScheduler 3.x per Pitfall 2). Set `result["scheduler"] = "running" if jobs else "no_jobs"`. No jobs = degraded.
       - Set `result["status"] = "degraded" if degraded else "ok"`
       - Set `response.status_code = 503` if degraded
       - Import `text` from `sqlalchemy`

    2. **Add `/metrics` endpoint** (per D-08 through D-12):
       - New route `@app.get("/metrics")` with `async def metrics() -> dict`
       - **Signal counts (D-09, D-11):** Use `async_session()`, query `select(SignalLog.signal_type, func.count(SignalLog.id)).where(SignalLog.created_at >= cutoff).group_by(SignalLog.signal_type)` where `cutoff = datetime.now(tz=timezone.utc) - timedelta(days=7)`. Build dict `{row[0]: row[1] for row in result.all()}`.
       - **Memory entries (D-09):** Query `select(func.count(MemoryFact.id))` via `scalar_one()`.
       - **Briefing latency (D-10):** Create Redis client, `async for key in redis.scan_iter("briefing:*:latency_s")`, read values, compute average. Close redis in `finally`.
       - Return shape per D-12: `{"briefing_latency_avg_s": <float>, "signals_7d": <dict>, "memory_entries": <int>}`
       - Import: `func, select` from `sqlalchemy`, `SignalLog` from `daily.profile.signals`, `MemoryFact` from `daily.db.models`, `datetime, timedelta, timezone` from `datetime`, `Redis as AsyncRedis` from `redis.asyncio`

    IMPORTANT: Do NOT instantiate Settings() at module level for Redis URL (per anti-pattern in RESEARCH.md). Instantiate inside the route function.
  </action>
  <verify>
    <automated>python -c "from daily.main import app; routes = [r.path for r in app.routes]; assert '/health' in routes; assert '/metrics' in routes; print('routes ok')"</automated>
  </verify>
  <done>GET /health probes DB, Redis, scheduler and returns ok/degraded with correct HTTP status. GET /metrics returns signal counts, memory entries, and latency average.</done>
  <acceptance_criteria>
    - [ ] `GET /health` probes DB (SELECT 1), Redis (PING), and scheduler (get_jobs)
    - [ ] `GET /health` returns `{"status": "ok", "db": "ok", "redis": "ok", "scheduler": "running"}` with HTTP 200 when all healthy
    - [ ] `GET /health` returns `{"status": "degraded", ...}` with HTTP 503 when any check fails
    - [ ] Redis client in /health is closed in a `finally` block
    - [ ] `GET /metrics` returns `{"briefing_latency_avg_s": ..., "signals_7d": {...}, "memory_entries": ...}`
    - [ ] Signal counts use 7-day rolling window (WHERE created_at >= now - 7 days)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Instrument briefing pipeline with latency timing and Redis write</name>
  <files>src/daily/briefing/pipeline.py</files>
  <read_first>
    - src/daily/briefing/pipeline.py — run_briefing_pipeline function, existing flow
    - .planning/phases/14-observability/14-RESEARCH.md — latency key write pattern, open question 2
    - .planning/phases/14-observability/14-CONTEXT.md — D-10 (latency stored in Redis per user)
  </read_first>
  <action>
    Modify `src/daily/briefing/pipeline.py` to record briefing generation latency in Redis:

    1. Add `import time` at module top.

    2. At the top of `run_briefing_pipeline()`, immediately after the opening `logger.info(...)` call, record `pipeline_start = time.monotonic()`.

    3. After the `cache_briefing()` call (Step 4) and the item cache block, but before the final `logger.info("Briefing pipeline complete...")`, add:
       ```python
       # Phase 14 OBS-04: record pipeline latency for /metrics aggregation (D-10)
       try:
           latency_s = time.monotonic() - pipeline_start
           await redis.set(
               f"briefing:{user_id}:latency_s",
               str(round(latency_s, 3)),
               ex=86400,  # 24h TTL, refreshed each precompute
           )
       except Exception as exc:
           logger.warning("Failed to write latency key: %s", exc)
       ```

    4. The latency key `briefing:{user_id}:latency_s` matches the pattern used by `/metrics` scan_iter.

    5. TTL of 86400 (24h) ensures stale latency data auto-expires if briefing stops running.

    IMPORTANT: Latency write failure must NOT block the pipeline (graceful degradation — same pattern as item cache block above it).
  </action>
  <verify>
    <automated>grep -n "latency_s" src/daily/briefing/pipeline.py</automated>
  </verify>
  <done>pipeline.py captures time.monotonic() at start, writes latency to Redis key briefing:{user_id}:latency_s with 24h TTL after cache_briefing(), failure is non-blocking</done>
  <acceptance_criteria>
    - [ ] `time.monotonic()` captured at start of `run_briefing_pipeline()`
    - [ ] `redis.set(f"briefing:{user_id}:latency_s", ...)` called after `cache_briefing()` with 24h TTL
    - [ ] Latency write wrapped in try/except — failure logged but does not raise
    - [ ] `pytest tests/test_briefing_pipeline.py -x` passes (existing tests still green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write test suites for /health and /metrics endpoints (OBS-03, OBS-04)</name>
  <files>tests/test_health_endpoint.py, tests/test_metrics_endpoint.py</files>
  <read_first>
    - tests/conftest.py — existing test fixtures
    - tests/test_main_lifespan.py — existing FastAPI test patterns, TestClient usage
    - src/daily/main.py — current app and endpoint structure (after Task 1 modifications)
    - .planning/phases/14-observability/14-VALIDATION.md — test map for OBS-03, OBS-04
  </read_first>
  <action>
    Create two test files using FastAPI TestClient with mocked dependencies.

    **`tests/test_health_endpoint.py`** (OBS-03):
    1. `test_health_all_ok` — mock `async_session` to succeed on SELECT 1, mock Redis ping to succeed, mock `scheduler.get_jobs()` to return `[mock_job]`. Assert response status 200, body `{"status": "ok", "db": "ok", "redis": "ok", "scheduler": "running"}`.
    2. `test_health_db_down` — mock `async_session` to raise `Exception("connection refused")`. Assert status 503, body has `"status": "degraded"`, `"db"` starts with `"error:"`.
    3. `test_health_redis_down` — mock Redis ping to raise. Assert status 503, `"redis"` starts with `"error:"`.
    4. `test_health_no_scheduler_jobs` — mock `scheduler.get_jobs()` to return `[]`. Assert `"scheduler": "no_jobs"`, status 503.

    **`tests/test_metrics_endpoint.py`** (OBS-04):
    1. `test_metrics_returns_all_fields` — mock DB session to return signal counts `[("skip", 3), ("expand", 10)]` and memory count `42`. Mock Redis scan_iter to yield one key with value `"4.5"`. Assert response 200, body has `briefing_latency_avg_s`, `signals_7d`, `memory_entries`.
    2. `test_metrics_empty_data` — mock DB to return empty results, mock Redis scan_iter to yield nothing. Assert `briefing_latency_avg_s == 0.0`, `signals_7d == {}`, `memory_entries == 0`.
    3. `test_metrics_latency_averages_multiple` — mock Redis scan_iter to yield 3 keys with values "2.0", "4.0", "6.0". Assert `briefing_latency_avg_s == 4.0`.

    Use `unittest.mock.patch` and `unittest.mock.AsyncMock` for async mocks. Match existing test patterns in the codebase (check conftest.py and test_main_lifespan.py for how the TestClient and mocking are set up).
  </action>
  <verify>
    <automated>pytest tests/test_health_endpoint.py tests/test_metrics_endpoint.py -x</automated>
  </verify>
  <done>4 health tests and 3 metrics tests pass. All mock external deps. Full suite green.</done>
  <acceptance_criteria>
    - [ ] `tests/test_health_endpoint.py` exists with 4 test functions covering ok and degraded paths
    - [ ] `tests/test_metrics_endpoint.py` exists with 3 test functions covering response shape and aggregation
    - [ ] All tests mock external dependencies (DB, Redis, scheduler) — no real connections needed
    - [ ] `pytest tests/test_health_endpoint.py tests/test_metrics_endpoint.py -x` passes
    - [ ] Full suite `pytest tests/ -x` passes (no regressions)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] OBS-03: GET /health returns 200 + ok body when all healthy
- [ ] OBS-03: GET /health returns 503 + degraded body when DB down
- [ ] OBS-03: GET /health returns 503 when scheduler has no jobs
- [ ] OBS-04: GET /metrics returns correct signal_7d counts
- [ ] OBS-04: GET /metrics reads latency from Redis and computes average
- [ ] OBS-04: pipeline.py writes latency_s key to Redis after run
</verification>

<success_criteria>
All 7 endpoint tests pass. /health correctly reports dependency status with appropriate HTTP codes. /metrics returns signal counts, memory size, and briefing latency. Pipeline records latency in Redis. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/14-observability/14-02-SUMMARY.md`
</output>
