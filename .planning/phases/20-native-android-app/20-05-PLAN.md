---
phase: 20
plan: "05"
type: execute
wave: 4
depends_on: ["20-01", "20-02", "20-03", "20-04"]
files_modified:
  - android/app/src/main/kotlin/com/daily/android/Config.kt
  - android/app/src/main/kotlin/com/daily/android/MainActivity.kt
  - android/app/src/main/AndroidManifest.xml
  - android/README.md
  - .planning/phases/20-native-android-app/20-VALIDATION.md
autonomous: false
requirements: [MOB-02]
must_haves:
  truths:
    - "Config.kt is the single source of truth for backendBaseURL and appLinksHost"
    - "No hardcoded https://app.example.com literal anywhere in android/app/src/main/kotlin (only Config.kt and AndroidManifest.xml literals remain — manifest cannot interpolate constants)"
    - "20-VALIDATION.md frontmatter has nyquist_compliant: true and wave_0_complete: true"
    - "Manual checkpoint passes on a physical Android device: pairing, voice round-trip, AEC, briefing audio"
  artifacts:
    - path: "android/app/src/main/kotlin/com/daily/android/Config.kt"
      provides: "object Config { backendBaseURL, appLinksHost }"
      contains: "object Config"
    - path: ".planning/phases/20-native-android-app/20-VALIDATION.md"
      provides: "Final validation matrix; signoff checkboxes ticked"
  key_links:
    - from: "MainActivity"
      to: "Config.backendBaseURL"
      via: "AuthService(Config.backendBaseURL, ...) + LiveKitTokenSource(Config.backendBaseURL)"
      pattern: "Config.backendBaseURL"
---

<objective>
Centralise the backend URL into a single Kotlin `Config` object, finalise `20-VALIDATION.md`, and run the manual device-test checkpoint that proves the Android voice loop on a real device. Mirror Phase 19 Plan 19-05 exactly.

Purpose: Remove duplicate URL literals introduced in 20-03/20-04 (mirror of T-19-25); finalise validation; gate the phase behind a real device test for AEC/briefing/UAT verification (no automated equivalent — RESEARCH §Validation Architecture).
Output: New Config.kt; updated MainActivity; finalised VALIDATION.md; signed-off manual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-native-android-app/20-RESEARCH.md
@.planning/phases/20-native-android-app/20-VALIDATION.md
@.planning/phases/19-native-ios-app/19-05-SUMMARY.md
@ios/dAIly/Config.swift
</context>

<interfaces>
Mirror of `ios/dAIly/Config.swift`:

```kotlin
package com.daily.android

object Config {
    /// Single source of truth for the backend host.
    /// Local dev: HTTPS tunnel (cloudflared / ngrok).
    /// Production: deployed FastAPI URL before Play Store.
    const val backendBaseURL: String = "https://app.example.com"

    /// Hostname for App Links autoVerify. Must match the host in
    /// AndroidManifest.xml `<data android:host="..."/>` AND the host
    /// serving /.well-known/assetlinks.json (Plan 20-01).
    const val appLinksHost: String = "app.example.com"
}
```

NOTE: Manifest `android:host` must remain a literal — Android's manifest does not interpolate Kotlin constants. Both literals (Config.appLinksHost + AndroidManifest android:host) must change together. The README warns about this.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Centralise backend URL in Config.kt + harden README</name>
  <read_first>
    - ios/dAIly/Config.swift — exact pattern to mirror
    - .planning/phases/19-native-ios-app/19-05-SUMMARY.md — Task 1 contract
    - android/app/src/main/kotlin/com/daily/android/MainActivity.kt — to remove BASE_URL literal
    - android/app/src/main/AndroidManifest.xml — to confirm host literal alignment
  </read_first>
  <action>
    1. Create `android/app/src/main/kotlin/com/daily/android/Config.kt` with the snippet in the interfaces block.

    2. Edit `android/app/src/main/kotlin/com/daily/android/MainActivity.kt`:
       - Remove any file-level `private const val BASE_URL = "https://app.example.com"`
       - Replace all usages with `Config.backendBaseURL`
       - Remove any `// TODO(20-05): ...` comments

    3. Confirm `AndroidManifest.xml` `android:host="app.example.com"` literal matches `Config.appLinksHost`.

    4. Update `android/README.md`:
       - Add a "Configuration" section: "All backend wiring lives in `Config.kt`. Edit `backendBaseURL` for tunnel/prod; edit `appLinksHost` AND `AndroidManifest.xml`'s `<data android:host=...>` together."
       - Add a "Local dev with cloudflared" step list mirroring `ios/README.md`:
         1. `cloudflared tunnel --url http://localhost:8000`
         2. Paste HTTPS URL into `Config.backendBaseURL`
         3. Paste host (without scheme) into `Config.appLinksHost` AND `AndroidManifest.xml` host literal
         4. `curl https://<tunnel>/.well-known/assetlinks.json` to verify Plan 20-01 endpoint reachable
         5. Reinstall debug APK so Android re-runs App Links verification
       - Add an "App Links debug verification" snippet:
         ```
         adb shell pm verify-app-links --re-verify com.daily.android
         adb shell pm get-app-links com.daily.android
         # Look for: 1024:com.daily.android (verified)
         ```
       - Add a warning callout: "When changing the backend host, you MUST update `Config.appLinksHost` AND the `android:host` literal in `AndroidManifest.xml` together — Android's manifest does not interpolate Kotlin constants."
  </action>
  <verify>
    <automated>test -f android/app/src/main/kotlin/com/daily/android/Config.kt && grep -q "Config.backendBaseURL" android/app/src/main/kotlin/com/daily/android/MainActivity.kt && ! grep -rE "https://app\\.example\\.com" android/app/src/main/kotlin/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f android/app/src/main/kotlin/com/daily/android/Config.kt`
    - `grep -nE "object Config" android/app/src/main/kotlin/com/daily/android/Config.kt`
    - `grep -nE "const val backendBaseURL" android/app/src/main/kotlin/com/daily/android/Config.kt`
    - `grep -nE "const val appLinksHost" android/app/src/main/kotlin/com/daily/android/Config.kt`
    - `grep -nE "Config.backendBaseURL" android/app/src/main/kotlin/com/daily/android/MainActivity.kt`
    - `! grep -rE "https://app\\.example\\.com" android/app/src/main/kotlin/` (no hardcoded URL outside Config.kt — note: kotlin tree only; manifest XML host literal is allowed and required)
    - `! grep -nE "private const val BASE_URL" android/app/src/main/kotlin/com/daily/android/MainActivity.kt`
    - `grep -q "cloudflared" android/README.md` (local-dev workflow documented)
    - `grep -q "verify-app-links" android/README.md` (debug verification documented)
    - `grep -q "Config.appLinksHost" android/README.md` (warning about manifest sync documented)
  </acceptance_criteria>
  <done>Single edit point for backend URL; MainActivity reads from Config; README documents tunnel workflow + manifest-sync warning + adb verification commands.</done>
</task>

<task type="auto">
  <name>Task 2: Finalise 20-VALIDATION.md</name>
  <read_first>
    - .planning/phases/20-native-android-app/20-VALIDATION.md — current draft
    - .planning/phases/19-native-ios-app/19-05-SUMMARY.md — proves the iOS finalised pattern (13-row map; 4 manual-only verifications)
    - All four prior plans (20-01 through 20-04) — to enumerate every task into the per-task verification map
  </read_first>
  <action>
    1. Edit frontmatter of `.planning/phases/20-native-android-app/20-VALIDATION.md`:
       - `status: approved`
       - `nyquist_compliant: true`
       - `wave_0_complete: true`
       - `approved: 2026-04-30` (date of plan execution)

    2. Replace the `## Per-Task Verification Map` "Populated during execution." placeholder with a full table covering EVERY task in plans 20-01 through 20-05. Columns: `Plan-Task | Behavior | Automated Command | Notes`. Minimum rows:
       - 20-01-01 (assetlinks endpoint) → `uv run pytest tests/test_assetlinks.py`
       - 20-02-01 (Gradle skeleton) → grep-based; build deferred
       - 20-02-02 (TokenStore) → `./gradlew testDebugUnitTest --tests "*.TokenStoreTest"`
       - 20-03-01 (PairCodeUriParser + AuthService) → `./gradlew testDebugUnitTest --tests "*.PairCodeUriParserTest" --tests "*.AuthServiceTest"`
       - 20-03-02 (TokenRefresher + FirstLaunchCleanup + AppLinks intent-filter + PairingScreen) → grep + manual link-tap test
       - 20-04-01 (LiveKitTokenSource) → `./gradlew testDebugUnitTest --tests "*.LiveKitTokenSourceTest"`
       - 20-04-02 (VoiceSession + DebugFlags) → `./gradlew testDebugUnitTest --tests "*.VoiceSessionTest"`
       - 20-04-03 (VoiceScreen + ConnectionIndicator + MainActivity wiring) → grep + manual UI test
       - 20-05-01 (Config.kt + README) → grep
       - 20-05-02 (this task)
       - 20-05-03 (manual checkpoint)

    3. Update `## Wave 0 Requirements` checklist — tick all boxes:
       - [x] `android/app/src/test/kotlin/com/daily/android/` exists
       - [x] JUnit 4 + MockK declared in build.gradle.kts
       - [x] MockWebServer declared
       - [x] Robolectric declared

    4. `## Manual-Only Verifications` already lists 3 — keep them; add a 4th: "Cold-launch < 3s (UAT)" mirroring iOS.

    5. `## Validation Sign-Off` — tick every box; add `**Approval:** approved 2026-04-30`.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/20-native-android-app/20-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/20-native-android-app/20-VALIDATION.md && grep -cE "^\\| 20-0[1-5]" .planning/phases/20-native-android-app/20-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^nyquist_compliant: true" .planning/phases/20-native-android-app/20-VALIDATION.md`
    - `grep -nE "^wave_0_complete: true" .planning/phases/20-native-android-app/20-VALIDATION.md`
    - `grep -nE "^status: approved" .planning/phases/20-native-android-app/20-VALIDATION.md`
    - `grep -cE "^\\| 20-0[1-5]" .planning/phases/20-native-android-app/20-VALIDATION.md` >= 11 (every task represented)
    - `grep -nE "Approval:.*approved" .planning/phases/20-native-android-app/20-VALIDATION.md`
    - `! grep -nE "Populated during execution" .planning/phases/20-native-android-app/20-VALIDATION.md` (placeholder gone)
  </acceptance_criteria>
  <done>VALIDATION.md is fully populated, signed off, and Nyquist-compliant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual device-test checkpoint</name>
  <what-built>
    Plans 20-01 through 20-05 Task 1+2 are complete. The Android app should now:
    - Pair via magic link (email → tap link → app opens → tokens persisted)
    - Connect to LiveKit room and play the daily briefing audio end-to-end
    - Eliminate echo via hardware AEC (default LiveKit audio module — no custom override)
    - Recover from network blips via Reconnecting → Listening
  </what-built>
  <how-to-verify>
    Pre-conditions:
    1. Backend running: `cd /Users/jacobmarriott/Documents/Personal/projects/dAIly && uv run uvicorn daily.main:app --reload --port 8000`
    2. LiveKit dev server running (Phase 18 setup)
    3. Tunnel: `cloudflared tunnel --url http://localhost:8000` — copy HTTPS URL
    4. `Config.kt` `backendBaseURL` AND `appLinksHost` set to tunnel URL/host
    5. `AndroidManifest.xml` `<data android:host="..."/>` set to tunnel host
    6. `.env` populated: `RESEND_API_KEY`, `MAGIC_LINK_BASE_URL`, `ANDROID_PACKAGE_NAME=com.daily.android`, `ANDROID_SHA256_FINGERPRINT=<your debug keystore SHA-256>`
    7. Verify assetlinks: `curl https://<tunnel>/.well-known/assetlinks.json` returns the expected JSON array
    8. Build + install: `cd android && ./gradlew installDebug`
    9. Verify App Links: `adb shell pm verify-app-links --re-verify com.daily.android && adb shell pm get-app-links com.daily.android` shows verified
    10. To force re-verification on subsequent builds, uninstall first: `adb uninstall com.daily.android`

    Device tests (run all on a physical Android device — emulator AEC unreliable):

    1. **Cold-launch + pairing**:
       - Open app → PairingScreen visible
       - Enter your email, tap "Send magic link"
       - Receive email; tap the link in Gmail (NOT a browser bookmark)
       - Verify the app opens directly (not a chooser/browser)
       - Verify the screen transitions to VoiceScreen with Idle state
       - **Pass criterion:** App opens directly to VoiceScreen, no browser chooser shown

    2. **Voice round-trip**:
       - Tap "Start" → state transitions Connecting → Listening (green circle within 3s)
       - Speak: "What's on my agenda today?"
       - State transitions Speaking (blue circle) when agent replies; audio plays through speaker
       - State returns to Listening after agent finishes
       - **Pass criterion:** Round-trip < 5s; audio audible; state machine visibly tracks turns

    3. **Hardware AEC (T-20-22)**:
       - Speaker volume at 70%+
       - Agent speaking (Speaking state) — try to interrupt by speaking over the agent
       - **Pass criterion:** Agent's voice is NOT picked up as user input (no echo loop, no false barge-in from agent's own audio)

    4. **Daily briefing playback**:
       - Trigger briefing flow per backend setup
       - Briefing audio plays end-to-end without dropouts
       - **Pass criterion:** Full briefing plays, no audio glitches

    5. **Reconnect**:
       - Mid-session, toggle airplane mode for 5s, then off
       - State transitions Listening → Reconnecting → Listening (within ~30s)
       - **Pass criterion:** Auto-recovery; no Error state

    6. **Stale token cleanup (T-20-13)**:
       - Uninstall app: `adb uninstall com.daily.android`
       - Reinstall: `./gradlew installDebug`
       - Open app → PairingScreen visible (NOT VoiceScreen)
       - **Pass criterion:** First-launch cleanup wiped tokens; user lands on PairingScreen

    7. **Cold-launch < 3s**:
       - Force-stop app, then tap launcher icon
       - **Pass criterion:** App reaches PairingScreen or VoiceScreen Idle within 3s
  </how-to-verify>
  <resume-signal>Type "approved" to mark phase 20 complete, or describe specific issues per device-test number.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → Production binary | URL literals must not leak duplicates that drift |
| Validation document → Phase signoff | Manual-only items must be enumerated; nothing slips silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-23 | Tampering | URL drift across files | mitigate | Single Config.kt source of truth; acceptance criteria asserts no `https://app.example.com` literal in Kotlin tree outside Config.kt |
| T-20-24 | Spoofing | Manifest host vs Config drift | mitigate | README warns; both literals MUST be changed together; manual checkpoint task 3.1 verifies App Links open the app (proves they match) |
| T-20-25 | Information Disclosure | Validation doc claims green without device test | mitigate | Manual checkpoint Task 3 is `gate="blocking"`; user must explicitly approve |
</threat_model>

<verification>
- All grep acceptance criteria pass
- VALIDATION.md is Nyquist-compliant
- Manual checkpoint reviewed and approved by user (Tasks 1–7)
</verification>

<success_criteria>
- One backend-URL literal in the Kotlin source tree (Config.kt)
- VALIDATION.md fully populated and signed off
- All 7 manual device tests pass on a physical Android device
- MOB-02 requirement closed
</success_criteria>

<output>
After completion, create `.planning/phases/20-native-android-app/20-05-SUMMARY.md`
</output>
