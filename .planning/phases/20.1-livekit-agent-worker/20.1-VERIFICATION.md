---
phase: 20.1-livekit-agent-worker
verified: 2026-05-01T00:00:00Z
status: human_needed
score: 5/6 must-haves verified
human_verification:
  - test: "Connect an Android device to the running docker stack and join a voice session room"
    expected: "Android client hears the cached briefing audio, can ask follow-up questions, and agent disconnects cleanly on hang-up"
    why_human: "SUMMARY confirms iOS smoke test passed; Android was explicitly not tested. Plan 20.1-04 Task 3 required both platforms. Cannot verify audio path, hardware AEC, or Oboe integration programmatically."
---

# Phase 20.1: LiveKit Agent Worker — Verification Report

**Phase Goal:** Build the server-side LiveKit Agents worker that dispatches into a user's room on connect, delivers the briefing from Redis as TTS audio via LiveKit, and handles voice turns via the existing LangGraph orchestrator — making the iOS and Android apps fully functional end-to-end.
**Verified:** 2026-05-01
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Worker process can be started with `python -m daily.worker` and registers with the LiveKit server | ✓ VERIFIED | `__main__.py` calls `cli.run_app(WorkerOptions(entrypoint_fnc=entrypoint))`; docker smoke test confirmed "registered worker" log within 15s (SUMMARY-04) |
| 2 | Worker resolves user_id from any room name matching `session-{user_id}-{ts}` | ✓ VERIFIED | `identity.py` uses strict `^session-(\d+)-\d+$` regex; 6 unit tests all pass |
| 3 | Worker loads per-user briefing and session state from Redis/Postgres via the existing orchestrator | ✓ VERIFIED | `state.py` `load_user_session_state` calls `initialize_session_state`, `build_graph`, `create_session_config`; 3 unit tests pass |
| 4 | Worker delivers briefing as TTS audio over LiveKit when a participant joins | ✓ VERIFIED | `agent.py` calls `agent.say(briefing, allow_interruptions=True)` after `VoicePipelineAgent.start(ctx.room)`; iOS smoke test confirmed audio playback (SUMMARY-04) |
| 5 | Agent handles voice turns via LangGraph (STT → LLM bridge → TTS) | ✓ VERIFIED | `voice_pipeline.py` wires `deepgram.STT(nova-3)` + `cartesia.TTS(sonic-2)` + `silero.VAD` + `DailyAgentLLM(bridge)`; `DailyLLMBridge.stream_response` routes to `astream_session`/`run_session`; 4+3 unit tests pass; iOS voice turns confirmed working (SUMMARY-04) |
| 6 | Worker runs as a containerized service in both dev and prod compose stacks | ✓ VERIFIED | `Dockerfile.worker` exists; `docker-compose.yml` and `docker-compose.prod.yml` both have `worker:` service; both validate cleanly (`docker compose config -q` exits 0) |

**Score:** 5/6 truths fully verified (truth 5 partial — Android voice path untested)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/daily/worker/__init__.py` | Package marker | ✓ VERIFIED | Exists |
| `src/daily/worker/identity.py` | `parse_user_id_from_room(room_name) -> int \| None` | ✓ VERIFIED | Substantive: strict regex, 13 lines. Wired: called in `agent.py` line 40 |
| `src/daily/worker/__main__.py` | CLI entrypoint calling `run_app(WorkerOptions(...))` | ✓ VERIFIED | Contains `cli.run_app(WorkerOptions(entrypoint_fnc=entrypoint))` |
| `src/daily/worker/agent.py` | Full entrypoint: connect → load state → speak briefing → voice loop | ✓ VERIFIED | Contains all required calls: `load_user_session_state`, `build_voice_pipeline`, `agent.say`, `allow_interruptions=True`, `parse_user_id_from_room`, `DailyLLMBridge` |
| `src/daily/worker/state.py` | `load_user_session_state(user_id) -> SessionBundle` async context manager | ✓ VERIFIED | `@asynccontextmanager`, `AsyncPostgresSaver`, `initialize_session_state`, `set_email_adapters` all present |
| `src/daily/worker/llm_bridge.py` | `DailyLLMBridge` adapter — routes to `astream_session`/`run_session` | ✓ VERIFIED | `class DailyLLMBridge`, `astream_session`, `run_session`, `StreamingNotSupported` all present |
| `src/daily/worker/voice_pipeline.py` | `build_voice_pipeline(bridge, settings) -> VoicePipelineAgent` + `DailyAgentLLM` | ✓ VERIFIED | `class DailyAgentLLM`, `deepgram.STT`, `cartesia.TTS`, `silero.VAD`, `DailyLLMBridge` all present |
| `tests/test_worker_identity.py` | 6 identity parser unit tests | ✓ VERIFIED | 6 passed |
| `tests/test_worker_state.py` | 3 session bootstrap unit tests | ✓ VERIFIED | 3 passed |
| `tests/test_worker_llm_bridge.py` | 3 LLM bridge unit tests | ✓ VERIFIED | 3 passed |
| `tests/test_worker_voice_pipeline.py` | 4 voice pipeline unit tests | ✓ VERIFIED | 4 passed |
| `Dockerfile.worker` | Container image for the worker | ✓ VERIFIED | Contains `python -m daily.worker`; `download-files` pre-run; CMD uses `start` |
| `docker-compose.yml` (worker service) | Worker service with depends_on + LIVEKIT_URL | ✓ VERIFIED | `worker:` service, `depends_on: [postgres, redis, livekit]`, `LIVEKIT_URL: ws://livekit:7880` |
| `docker-compose.prod.yml` (worker service) | Prod worker with `restart: unless-stopped` | ✓ VERIFIED | `worker:` service, `restart: unless-stopped`, env var substitution |
| `.planning/phases/20.1-livekit-agent-worker/20.1-SMOKE-TEST.md` | End-to-end smoke test runbook | ✓ VERIFIED | Contains `## Steps` section; iOS smoke test executed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `__main__.py` | `agent.py` | `WorkerOptions(entrypoint_fnc=entrypoint)` | ✓ WIRED | Line 20 of `__main__.py` |
| `agent.py` | `identity.py` | `parse_user_id_from_room(ctx.room.name)` | ✓ WIRED | Line 40 of `agent.py` |
| `agent.py` | `state.py` | `async with load_user_session_state(user_id)` | ✓ WIRED | Line 65 of `agent.py` |
| `agent.py` | `voice_pipeline.py` | `build_voice_pipeline(bridge, settings)` | ✓ WIRED | Line 76 of `agent.py` |
| `state.py` | `orchestrator/graph.py` | `build_graph(checkpointer=checkpointer)` | ✓ WIRED | Line 59 of `state.py` |
| `state.py` | `orchestrator/session.py` | `initialize_session_state(user_id, redis, db_sess)` | ✓ WIRED | Line 64 of `state.py` |
| `llm_bridge.py` | `orchestrator/session.py` | `astream_session` / `run_session` | ✓ WIRED | Lines 49, 53 of `llm_bridge.py` |
| `voice_pipeline.py` | `livekit.plugins.deepgram` | `deepgram.STT(model="nova-3", ...)` | ✓ WIRED | Line 102 of `voice_pipeline.py` |
| `voice_pipeline.py` | `livekit.plugins.cartesia` | `cartesia.TTS(model="sonic-2", ...)` | ✓ WIRED | Line 103 of `voice_pipeline.py` |
| `docker-compose.yml` worker | `docker-compose.yml` livekit | `depends_on: [livekit]` | ✓ WIRED | Worker service depends_on includes livekit |
| `docker-compose.yml` worker | `src/daily/worker/__main__.py` | `command: python -m daily.worker dev` | ✓ WIRED | Command present in worker service |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `agent.py` | `bundle.briefing_narrative` | `initialize_session_state` → Redis cache | Redis `GET briefing:{user_id}` (existing Phase 18 pipeline) | ✓ FLOWING |
| `llm_bridge.py` | token deltas from `stream_response` | `astream_session` → LangGraph graph | Real LangGraph compiled graph with Postgres checkpointer | ✓ FLOWING |
| `voice_pipeline.py` | audio output | `cartesia.TTS` via `DailyAgentLLM` → `DailyLLMBridge` | Real Cartesia API call with `settings.cartesia_api_key` | ✓ FLOWING (API key required at runtime) |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 16 worker unit tests pass | `uv run pytest tests/test_worker*.py -v` | 16 passed in 3.04s | ✓ PASS |
| Docker Compose YAML is valid | `docker compose config -q` | exits 0 | ✓ PASS |
| Prod Compose YAML is valid | `docker compose -f docker-compose.prod.yml config -q` | exits 0 | ✓ PASS |
| Worker module importable (with PYTHONPATH=src) | `PYTHONPATH=src uv run python -c "from daily.worker.agent import entrypoint; print('ok')"` | prints "ok" | ✓ PASS |
| iOS end-to-end smoke test | Manual — see SUMMARY-04 | Briefing audio heard; voice turns worked; auth bugs fixed during test | ✓ PASS (iOS) |
| Android end-to-end smoke test | Manual — not run | Android client not tested | ? SKIP |

**Note on module import:** `python -m daily.worker` requires `PYTHONPATH=src` in the local dev environment (or the package installed via `uv sync`). In the Docker container, `uv sync` installs the package from `pyproject.toml` (`packages = ["src/daily"]`), so the command works correctly there. This is expected behaviour and not a gap.

---

### Requirements Coverage

No separate `REQUIREMENTS.md` file exists in `.planning/`. Requirement IDs are defined inline in `ROADMAP.md` for Phase 20.1. Coverage assessed against the plan-level claim for each ID:

| Requirement | Source Plan | Description (derived from plan objective) | Status | Evidence |
|-------------|-------------|-------------------------------------------|--------|----------|
| WORKER-01 | 20.1-01 | `daily.worker` package scaffold + identity parser + CLI launcher | ✓ SATISFIED | `identity.py`, `__main__.py`, `agent.py`, `__init__.py` all exist and are substantive; 6 identity tests pass |
| WORKER-02 | 20.1-02 | Per-user session bootstrap (`load_user_session_state`) + LangGraph LLM bridge (`DailyLLMBridge`) | ✓ SATISFIED | `state.py` and `llm_bridge.py` substantive; 6 unit tests pass |
| WORKER-03 | 20.1-03 | Voice pipeline factory (`build_voice_pipeline`) wiring Deepgram + Cartesia + Silero + DailyAgentLLM | ✓ SATISFIED | `voice_pipeline.py` wires all four components; 4 unit tests pass |
| WORKER-04 | 20.1-03 | Full agent entrypoint: connect → briefing → voice loop → disconnect | ✓ SATISFIED | `agent.py` full entrypoint verified; iOS smoke test confirmed end-to-end audio |
| WORKER-05 | 20.1-04 | Dockerized worker registered in dev + prod compose stacks; smoke test runbook; mobile client verified | ⚠️ PARTIAL | Docker artifacts verified; iOS confirmed; Android not tested per SUMMARY-04 deviation note |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No TODOs, stubs, empty returns, or hardcoded empty data found in worker package | — | — |

No anti-patterns detected across `src/daily/worker/*.py`. The `agent.py` stub from Plan 01 was fully replaced in Plan 03. All handler paths produce real behaviour.

---

### Human Verification Required

#### 1. Android End-to-End Voice Session

**Test:** With `docker compose up -d` running and a briefing seeded in Redis for `user_id=1`, connect an Android device (with a configured `Config.backendBaseURL` pointing at the dev stack), pair via magic-link, and start a voice session from the Android app.

**Expected:**
- Android client state transitions: `.connecting` → `.listening` → `.speaking`
- Briefing audio is heard through the Android speaker (Cartesia TTS via LiveKit)
- Saying a follow-up question produces a coherent spoken response within ~3s
- Saying "exit" or hanging up causes the agent to disconnect cleanly
- `docker compose logs worker` shows no `Traceback` or 5xx errors during the session

**Why human:** The iOS smoke test passed (confirmed in SUMMARY-04). Android was explicitly deferred — "iOS only (Android not tested)" is recorded as a deviation. The Android audio path uses Oboe hardware AEC and the Kotlin LiveKit SDK, which cannot be verified programmatically. Physical device + microphone required.

---

### Gaps Summary

No automated gaps. The phase goal is substantively met for the iOS client path. The only outstanding item is the Android end-to-end test, which requires a physical Android device. All 16 unit tests pass, all artifacts are substantive and wired, all Docker artifacts are valid, and the iOS smoke test confirmed full end-to-end audio delivery including voice turns and clean disconnect.

WORKER-05 is rated partial rather than satisfied because Plan 20.1-04 Task 3 explicitly required "both iOS and Android end-to-end tests passed" and the SUMMARY records "approved (ios only)" status. Once Android is verified, the phase can be marked fully complete.

---

_Verified: 2026-05-01_
_Verifier: Claude (gsd-verifier)_
