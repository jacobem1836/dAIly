---
phase: 20.1-livekit-agent-worker
plan: "01"
subsystem: worker
tags: [livekit, agents, worker, identity]
dependency_graph:
  requires: []
  provides: [daily.worker, parse_user_id_from_room, python -m daily.worker CLI]
  affects: [pyproject.toml]
tech_stack:
  added: [livekit-agents>=0.12, livekit-plugins-deepgram>=0.6, livekit-plugins-cartesia>=0.4, livekit-plugins-silero>=0.7, av (via livekit-agents[codecs])]
  patterns: [TDD red-green, LiveKit WorkerOptions entrypoint pattern]
key_files:
  created:
    - src/daily/worker/__init__.py
    - src/daily/worker/identity.py
    - src/daily/worker/agent.py
    - src/daily/worker/__main__.py
    - tests/test_worker_identity.py
  modified:
    - pyproject.toml
key_decisions:
  - "livekit-agents[codecs] extra chosen over bare livekit-agents to pull in av (PyAV) which is a hard transitive dep of the codec decoder module"
  - "parse_user_id_from_room uses strict ^session-(\\d+)-\\d+$ regex per T-20.1-01 threat mitigation — rejects any non-conforming room name"
metrics:
  duration: "~15 minutes"
  completed: "2026-04-30"
  tasks_completed: 2
  tasks_total: 2
  files_created: 5
  files_modified: 1
---

# Phase 20.1 Plan 01: livekit-agents SDK + Worker Package Summary

**One-liner:** livekit-agents SDK + plugins installed; `daily.worker` package scaffolded with strict room-name identity parser and CLI launcher; `python -m daily.worker --help` enumerates LiveKit subcommands.

## What Was Built

### Task 1: livekit-agents dependencies + identity parser (TDD)

- Added `livekit-agents[codecs]>=0.12,<1.0`, `livekit-plugins-deepgram>=0.6`, `livekit-plugins-cartesia>=0.4`, `livekit-plugins-silero>=0.7` to `pyproject.toml`
- Created `src/daily/worker/__init__.py` (empty package marker)
- Created `src/daily/worker/identity.py` — `parse_user_id_from_room(room_name: str) -> int | None` using `^session-(\d+)-\d+$` regex
- Created `tests/test_worker_identity.py` — 6 tests (standard, single-digit, non-session, non-integer segment, empty string, empty segment) — all pass

### Task 2: Worker entrypoint shell + CLI launcher

- Created `src/daily/worker/agent.py` — `async def entrypoint(ctx: JobContext)`: connects to room, resolves user_id via `parse_user_id_from_room`, shuts down on invalid room name, waits for participant
- Created `src/daily/worker/__main__.py` — `main()` surfaces `LIVEKIT_URL/API_KEY/API_SECRET` from `Settings` into env, calls `cli.run_app(WorkerOptions(entrypoint_fnc=entrypoint))`
- `python -m daily.worker --help` enumerates: `dev`, `start`, `connect`, `download-files`

## Verification Results

- `uv run pytest tests/test_worker_identity.py` — 6 passed
- `uv run python -m daily.worker --help` — exits 0, shows dev/start subcommands
- `grep -q "livekit-agents" pyproject.toml` — exits 0
- All 4 missing-artifact checks pass (`run_app`, `async def entrypoint`, `parse_user_id_from_room`, `WorkerOptions`)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added `av` (PyAV) via `livekit-agents[codecs]` extra**
- **Found during:** Task 2 smoke test
- **Issue:** `livekit.agents.utils.codecs.decoder` imports `av` at module load time; bare `livekit-agents` does not pull it in as a required dep — importing any agents module raised `ModuleNotFoundError: No module named 'av'`
- **Fix:** Changed `livekit-agents>=0.12,<1.0` to `livekit-agents[codecs]>=0.12,<1.0` in pyproject.toml, which declares `av` as a required extra
- **Files modified:** `pyproject.toml`
- **Commit:** 1230e62

## Known Stubs

None — no UI rendering paths, no data flowing to frontend. Worker shell intentionally defers voice logic to Plan 20.1-02.

## Threat Flags

None — no new network endpoints or trust boundaries introduced beyond what the plan's threat model covers (T-20.1-01, T-20.1-02, T-20.1-03 all addressed).

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 | 2bfe1be | feat(20.1-01): add livekit-agents deps + worker identity parser |
| 2 | 1230e62 | feat(20.1-01): worker entrypoint shell + CLI launcher |

## Self-Check: PASSED

- `src/daily/worker/__init__.py` — exists
- `src/daily/worker/identity.py` — exists
- `src/daily/worker/agent.py` — exists
- `src/daily/worker/__main__.py` — exists
- `tests/test_worker_identity.py` — exists
- Commits 2bfe1be and 1230e62 — present in git log
