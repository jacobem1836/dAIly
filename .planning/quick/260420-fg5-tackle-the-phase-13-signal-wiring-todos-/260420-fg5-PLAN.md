---
phase: quick
plan: 260420-fg5
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "STATE.md no longer lists stale Phase 13 signal wiring todos"
    - "No code changes needed — all three todos were completed during Phase 13 execution"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Clean pending todos section"
  key_links: []
---

<objective>
Remove three stale Phase 13 todos from STATE.md that were already completed during Phase 13 execution.

Purpose: The three pending todos in STATE.md were written during Phase 13 planning but the work was fully completed across plans 13-01, 13-02, and 13-03. The Phase 13 VERIFICATION.md confirms 9/9 must-haves verified and all 3 requirements (SIG-01, SIG-02, SIG-03) satisfied. These todos are now stale and should be cleaned up to avoid confusion.

Evidence that all three are already done:
1. "Fire skip signals from voice session loop" -- `src/daily/voice/loop.py` line 268 fires `_capture_signal_inline(user_id, SignalType.skip, target_id=sender)` on barge-in + 2s silence (implicit skip)
2. "Fire re_request signals when user asks to repeat an item" -- `src/daily/orchestrator/nodes.py` line 336-337 fires `_capture_signal(state.active_user_id, SignalType.re_request, target_id=sender)` in `re_request_node`, routed by `graph.py` `re_request_keywords`
3. "Update adaptive ranker decay computation to include skip + re_request" -- `src/daily/profile/adaptive_ranker.py` line 25-29 has `SIGNAL_WEIGHTS` including `SignalType.skip: -1.0`, `SignalType.re_request: 1.0`, and `SignalType.expand: 0.5`

Output: Clean STATE.md with empty Pending Todos section.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/13-signal-capture/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale Phase 13 todos from STATE.md</name>
  <files>.planning/STATE.md</files>
  <action>
Edit `.planning/STATE.md` to remove the three stale Phase 13 pending todos:

In the `### Pending Todos` section, replace the three bullet points:
```
- Phase 13: Fire skip signals from voice session loop (skip node currently wired but signal not written)
- Phase 13: Fire re_request signals when user asks to repeat an item
- Phase 13: Update adaptive ranker decay computation to include skip + re_request alongside expand
```

With:
```
None.
```

These todos were completed during Phase 13 execution (confirmed by 13-VERIFICATION.md: 9/9 must-haves, 3/3 requirements satisfied). They were never removed from STATE.md during milestone closeout.

Also update the `Session Continuity` section at the bottom:
- Change `Next:` line to: `Next: Start v1.3 milestone planning.`
  </action>
  <verify>
    <automated>cd /Users/jacobmarriott/Documents/Personal/projects/dAIly && grep -c "Phase 13:" .planning/STATE.md | xargs -I{} test {} -eq 0 && echo "OK: no stale Phase 13 todos" || echo "FAIL: stale todos remain"</automated>
  </verify>
  <done>STATE.md Pending Todos section is clean — no stale Phase 13 references remain. Session continuity updated.</done>
</task>

</tasks>

<verification>
- `grep "Phase 13:" .planning/STATE.md` returns no matches
- `grep "None" .planning/STATE.md` shows "None." under Pending Todos
- No code files are modified (all signal wiring was already complete)
</verification>

<success_criteria>
- STATE.md has no Phase 13 pending todos
- No code changes made (all work was already done)
- STATE.md accurately reflects project state
</success_criteria>

<output>
No SUMMARY needed — this is a STATE.md cleanup task only.
</output>
